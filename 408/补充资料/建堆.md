1. #建堆 
	这是一个非常重要的考点。
	*   **方法一**：逐个插入$n$个元素。平均时间$O(n)$（基于上述$O(1)$的结论），最坏时间$O(n\log n)$。
	*   方法二（ #Floyd建堆算法  /筛选法）**：从最后一个非叶子节点开始，自下而上进行下滤（Sift Down）。
	    *   时间复杂度分析：这是一个经典的数学推导。总代价为$\sum_{h=0}^{\log n} \frac{n}{2^{h+1}} \cdot h = O(n)$。
	    *   **结论**：无论输入数据如何，线性建堆的时间复杂度稳定为$O(n)$。
