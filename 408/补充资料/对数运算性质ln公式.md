1. #对数运算性质ln公式  #ln公式  
	*   幂规则：$\lg(a^b) = b\lg a$。这是本题化简指数的关键。
    *   换底公式推论：$a^{\log_b c} = c^{\log_b a}$（虽然本题解析没用这个，但也是常用解法，例如$n^{\lg n} = n^{\lg n}$无法直接化简，但常用于$n^{\lg a} = a^{\lg n}$）。  
    *   恒等式：$a = b^{\log_b a}$。