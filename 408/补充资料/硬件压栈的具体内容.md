#硬件压栈 的具体内容 
1. 发生中断时，堆栈中从栈顶到栈底的顺序是什么？
	*   无特权级切换：$EIP\rightarrow CS\rightarrow EFLAGS$
    *   有特权级切换（用户态到内核态）：$EIP\rightarrow CS\rightarrow EFLAGS\rightarrow ESP\rightarrow SS$
**注意**：EAX, ECX 等通用寄存器**不在**硬件压栈列表中


如程序计数器（`EIP`）、代码段寄存器（`CS`）和标志寄存器（`EFLAGS`）。如果发生了特权级切换，还会保存旧的栈指针（`SS`和`ESP`）。