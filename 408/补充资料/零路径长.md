一个节点的 #零路径长 npl(x) 指的是从该节点 x 出发，到达一个**没有**左孩子或右孩子（即外部节点或null节点）的 #最短路径长度 。

*   对于一个空节点（null），我们定义其 $npl = -1$。
*   对于一个非空节点 x，其 npl 的计算公式为：
    $npl(x) = 1 + \min(npl(x.\text{left}), npl(x.\text{right}))$
