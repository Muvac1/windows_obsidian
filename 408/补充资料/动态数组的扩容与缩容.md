#动态数组的扩容与缩容    [[装填因子]]  
动态数组（Vector）的扩容与缩容
*   **扩容（Expansion）：** 当数组空间不足时，申请一块更大的连续空间（通常是当前容量的2倍），将原数据拷贝过去，释放原空间。
*   **缩容（Shrinking）：** 当数组空间利用率过低时，为了节省内存，申请一块较小的空间，搬运数据并释放大空间。


[[装填因子]]    
定义为 $\lambda = \frac{size}{capacity}$。
*   扩容通常发生在 $\lambda = 1$ 时。
*   缩容阈值的设定是算法效率的关键。

