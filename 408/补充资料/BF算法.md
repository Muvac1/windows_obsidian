1. #BF算法  #朴素模式匹配算法（BF算法 
	*   **原理**：从主串的第一个字符开始，与模式串的第一个字符比较，若相等则继续逐个比较；若不等，则主串指针回溯到下一个位置，模式串指针回溯到头，重新开始。
    *   **最坏复杂度**：$O(n\times m)$。出现在主串和模式串充斥着大量重复字符时（如主串`AAAA...A`，模式串`AAAB`）。
    *   **平均复杂度**：$O(n+m)$。在随机数据下，BF算法效率其实很高。