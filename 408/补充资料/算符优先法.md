1. #算符优先法  #中缀转后缀  
	这是编译器设计和计算器程序中的基础算法。
    *   **中缀表达式**（如 $3 + 4$）通常需要转换为 **后缀表达式**（逆波兰式，如 $3 \ 4 \ +$）或者使用双栈法直接求值。
    *   **单目运算符与双目运算符**：
        *   单目（Unary）：如 `!` (阶乘)，只需要弹出一个操作数。
        *   双目（Binary）：如 `+`, `*`，需要弹出两个操作数。注意运算顺序，通常是 $\text{次顶元素} \ \text{op} \ \text{栈顶元素}$。






