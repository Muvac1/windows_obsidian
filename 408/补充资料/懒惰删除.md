#懒惰删除  [[散列函数Hash哈希函数]]  [[散列表处理冲突的方法]]       [[开放定址法]]   [[平方探测法]] 
在 #开放定址法 的散列表中，不能直接物理删除元素，因为这会截断后续元素的查找探测链。
**做法：** 仅给该位置打上一个“已删除”（Deleted/Tombstone）的标记。
**影响：** 在查找时，遇到“已删除”标记继续向后探测；在插入时，遇到“已删除”标记可以覆盖并存入新元素。但在计算装填因子$\lambda$以判断是否需要扩容（Rehashing）时，“已删除”的元素通常也被计入“占用”状态，因为它们依然占据探测路径。