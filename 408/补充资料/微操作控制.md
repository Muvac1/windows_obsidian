#微操作控制 #微指令操作控制字段的编码方式    [[总线]] 
	控制器（CU）在每个时钟周期发出不同的信号组合（如 `PCout`, `ALUop`），指挥数据流动。
*   **考点衍生**：如果改为**双总线**或**三总线**结构，上述指令的周期数会如何变化？
    *   *例如：三总线结构中，`inc R1` 可能只需要 1 个周期（读 R1 -> ALU -> 写回 R1 可以流水或在一个长周期内完成，取决于设计）。*