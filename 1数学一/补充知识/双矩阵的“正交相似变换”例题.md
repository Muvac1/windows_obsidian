这里为你设计一道经典的“双矩阵正交相似”题目，并结合“桥梁传递法”给出详细解答，最后深度解析那个**致命坑点**。

---

### 📝 模拟真题

设两个 3 阶实对称矩阵 $A$ 和 $B$ 分别为：
$$ A = \begin{pmatrix} 2 & 1 & 0 \\ 1 & 2 & 0 \\ 0 & 0 & 1 \end{pmatrix}, \quad B = \begin{pmatrix} 1 & 0 & 0 \\ 0 & 2 & 1 \\ 0 & 1 & 2 \end{pmatrix} $$
求一个正交矩阵 $Q$，使得 $B = Q^T A Q$。

---

### 💡 题目解析与步骤

#### 第一步：求 $A$ 的特征值与特征向量，构建 $Q_A$

1.  **求特征值**： #特征多项式 
    $$ |\lambda E - A| = \begin{vmatrix} \lambda-2 & -1 & 0 \\ -1 & \lambda-2 & 0 \\ 0 & 0 & \lambda-1 \end{vmatrix} = (\lambda-1)[(\lambda-2)^2 - 1] = (\lambda-1)(\lambda-1)(\lambda-3) $$
    $A$ 的特征值为 $\lambda_1 = 1, \lambda_2 = 1, \lambda_3 = 3$。
[[特征多项式的原因]] 
2.  **求特征向量并正交单位化**：
    *   **当 $\lambda = 1$ (重根) 时**：
        解方程 $(E-A)x=0$，得基础解系 $\xi_1 = (1, -1, 0)^T, \xi_2 = (0, 0, 1)^T$。
        （注：这两个向量已经正交，只需单位化）
        单位化得：$\eta_1 = \frac{1}{\sqrt{2}}(1, -1, 0)^T, \quad \eta_2 = (0, 0, 1)^T$。
    *   **当 $\lambda = 3$ (单根) 时**：
        解方程 $(3E-A)x=0$，得基础解系 $\xi_3 = (1, 1, 0)^T$。
        单位化得：$\eta_3 = \frac{1}{\sqrt{2}}(1, 1, 0)^T$。

3.  **构建 $Q_A$**：
    我们约定对角矩阵 $\Lambda = \text{diag}(1, 1, 3)$。
    则 $Q_A = (\eta_1, \eta_2, \eta_3) = \begin{pmatrix} \frac{1}{\sqrt{2}} & 0 & \frac{1}{\sqrt{2}} \\ -\frac{1}{\sqrt{2}} & 0 & \frac{1}{\sqrt{2}} \\ 0 & 1 & 0 \end{pmatrix}$。
    满足 $Q_A^T A Q_A = \Lambda$。

#### 第二步：求 $B$ 的特征值与特征向量，构建 $Q_B$

1.  **求特征值**：
    计算可得 $B$ 的特征值也为 $\lambda = 1, 1, 3$。（若特征值不同，则 $A, B$ 不相似，题目无解）。

2.  **求特征向量并正交单位化**：
    *   **当 $\lambda = 1$ (重根) 时**：
        解方程 $(E-B)x=0$，得 #基础解系 $\alpha_1 = (1, 0, 0)^T, \alpha_2 = (0, 1, -1)^T$。
        单位化得：$\beta_1 = (1, 0, 0)^T, \quad \beta_2 = \frac{1}{\sqrt{2}}(0, 1, -1)^T$。
    *   **当 $\lambda = 3$ (单根) 时**：
        解方程 $(3E-B)x=0$，得基础解系 $\alpha_3 = (0, 1, 1)^T$。
        单位化得：$\beta_3 = \frac{1}{\sqrt{2}}(0, 1, 1)^T$。

3.  **构建 $Q_B$ (关键步骤！)**：
    为了使 $Q_B^T B Q_B = \Lambda$，**列向量的排列顺序必须与 $A$ 保持完全一致**。
    因为 $\Lambda = \text{diag}(1, 1, 3)$，所以 $Q_B$ 的前两列必须是 $\lambda=1$ 的特征向量，第三列必须是 $\lambda=3$ 的特征向量。
    $$ Q_B = (\beta_1, \beta_2, \beta_3) = \begin{pmatrix} 1 & 0 & 0 \\ 0 & \frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}} \\ 0 & -\frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}} \end{pmatrix} $$

#### 第三步：利用桥梁公式求 $Q$

根据推导：
$$ \begin{cases} A = Q_A \Lambda Q_A^T \\ B = Q_B \Lambda Q_B^T \end{cases} \implies \Lambda = Q_A^T A Q_A $$
代入 $B$ 的式子：
$$ B = Q_B (Q_A^T A Q_A) Q_B^T = (Q_B Q_A^T) A (Q_A Q_B^T) $$
题目要求 $B = Q^T A Q$，对比可知：
$$ Q = Q_A Q_B^T $$

**计算矩阵乘法**：
$$ Q = \begin{pmatrix} \frac{1}{\sqrt{2}} & 0 & \frac{1}{\sqrt{2}} \\ -\frac{1}{\sqrt{2}} & 0 & \frac{1}{\sqrt{2}} \\ 0 & 1 & 0 \end{pmatrix} \begin{pmatrix} 1 & 0 & 0 \\ 0 & \frac{1}{\sqrt{2}} & -\frac{1}{\sqrt{2}} \\ 0 & \frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}} \end{pmatrix} $$
*(注意：这里乘的是 $Q_B^T$，不要忘记转置 $Q_B$)*

计算结果：
$$ Q = \begin{pmatrix} \frac{1}{\sqrt{2}} & \frac{1}{2} & \frac{1}{2} \\ -\frac{1}{\sqrt{2}} & \frac{1}{2} & \frac{1}{2} \\ 0 & -\frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}} \end{pmatrix} $$

---

### 🛑 致命坑点深度解析：特征值顺序错位

在考场上，90% 做错这道题的同学都是死在 **$\Lambda$ 的顺序不统一** 上。

#### ❌ 错误示范：
假设你在做 $Q_A$ 时，按 $\Lambda = \text{diag}(1, 1, 3)$ 排列，得到 $Q_A = (\eta_1, \eta_2, \eta_3)$。
但在做 $Q_B$ 时，你随意排列，先把单根算出来了，写成了 $Q_B = (\beta_3, \beta_1, \beta_2)$，对应的对角矩阵变成了 $\Lambda' = \text{diag}(3, 1, 1)$。

此时：
*   $Q_A$ 把 $A$ 变成了 $\text{diag}(1, 1, 3)$
*   $Q_B$ 把 $B$ 变成了 $\text{diag}(3, 1, 1)$

当你计算 $Q = Q_A Q_B^T$ 时，你实际上是在试图建立一个映射，把 $A$ 的特征值 $1$ 的空间映射到 $B$ 的特征值 $3$ 的空间。
**后果**：计算出的 $Q$ 依然是正交矩阵，但 **$Q^T A Q \neq B$**。你会得到一个对角线元素乱序的矩阵，不仅拿不到分，而且验算时会让你怀疑人生。

#### ✅ 正确姿势（避坑指南）：
1.  **先定 $\Lambda$**：在草稿纸上写下大大的 $\Lambda = \text{diag}(\lambda_1, \lambda_2, \dots)$。
2.  **强制对齐**：
    *   构造 $Q_A$ 时，第 $i$ 列必须是对应 $\lambda_i$ 的特征向量。
    *   构造 $Q_B$ 时，第 $i$ 列**也必须**是对应 $\lambda_i$ 的特征向量。
3.  **重根内部无所谓**：如果 $\lambda_1 = \lambda_2 = 1$，那么 $Q_A$ 的前两列只要是 $\lambda=1$ 的特征向量即可，这两列内部左右互换不影响最终结果的正确性（虽然求出的 $Q$ 会不同，但都满足题目要求）。