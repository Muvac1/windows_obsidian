![[2017年考研数学（一）真题.pdf#page=1&rect=5,620,590,713|2017年考研数学（一）真题, p.1]]
[[Pasted image 20251004022107.png]]
#导数的定义    [[可微、连续、、可导、偏导数存在的关系]]      
#导函数的连续性    [[导数性质（连续性，判断是否可导）]]
	$\lim_{x \to 0^-} f(x) = \lim_{x \to 0^+} f(x) = f(0)$ 
1.  第一步：求函数在 $x=0$ 处的值和左极限 
	1. $f(0) = b$ 
	2. $\lim_{x \to 0^-} f(x) = \lim_{x \to 0^-} b = b$ 
2. 第二步：求函数在 $x=0$ 处的右极限 
	1. $\lim_{x \to 0^+} f(x) = \lim_{x \to 0^+} \frac{1 - \cos\sqrt{x}}{ax}$ 
    2.  [[无穷小公式补充]]     $1 - \cos u \sim \frac{1}{2}u^2$
	3. 当$x \to 0^+$时，$\sqrt{x} \to 0$。我们可以令$u = \sqrt{x}$，套用公式$1 - \cos u \sim \frac{1}{2}u^2$。
		所以，$1 - \cos\sqrt{x} \sim \frac{1}{2}(\sqrt{x})^2 = \frac{1}{2}x$ 
	4. 将分子替换掉，重新计算极限：
		$\lim_{x \to 0^+} \frac{1 - \cos\sqrt{x}}{ax} = \lim_{x \to 0^+} \frac{\frac{1}{2}x}{ax} = \lim_{x \to 0^+} \frac{1}{2a} = \frac{1}{2a}$ 
3. $b = \frac{1}{2a}$  
	1. $ab = \frac{1}{2}$(A) 
	
- 衍生 
	- #洛必达法则  
		- $\frac{d}{dx}(1 - \cos\sqrt{x}) = -(-\sin\sqrt{x}) \cdot (\frac{1}{2\sqrt{x}}) = \frac{\sin\sqrt{x}}{2\sqrt{x}}$  分子求导
		应用法则后，极限变为：$\lim_{x \to 0^+} \frac{\frac{\sin\sqrt{x}}{2\sqrt{x}}}{a} = \frac{1}{a} \lim_{x \to 0^+} \frac{\sin\sqrt{x}}{2\sqrt{x}}$
		*   这里又遇到了一个 #重要极限$\lim_{u \to 0} \frac{\sin u}{u} = 1$。令$u=\sqrt{x}$，则极限为：
		    $\frac{1}{a} \cdot \frac{1}{2} \cdot \lim_{x \to 0^+} \frac{\sin\sqrt{x}}{\sqrt{x}} = \frac{1}{2a} \cdot 1 = \frac{1}{2a}$ 
	 -  [[导数性质（连续性，判断是否可导）]] 
		 - 可导的前提是连续
		 - 可导的定义是左导数等于右导数，即$f'_{-}(0) = f'_{+}(0)$

![[Pasted image 20251004021715.png]]
[[Pasted image 20251004022115.png]]

表达式变化 

1. #构造函数法   
	1. 构造$g(x)=\frac{1}{2}[f(x)]^2$ 
	2. 对$g(x)=\frac{1}{2}[f(x)]^2$求导，根据 #链式法则 得到：
	    $g'(x) = \frac{d}{dx}\left(\frac{1}{2}[f(x)]^2\right) = \frac{1}{2} \cdot 2f(x) \cdot f'(x) = f(x)f'(x)$ 
		1. 有$g'(x)>0$ 
		2. 因此，函数$g(x)=\frac{1}{2}[f(x)]^2$是严格单调递增的。同理，函数$[f(x)]^2$也是严格单调递增的
	3. 比较$x=1$和$x=-1$处的函数值。因为$1>-1$，所以：
	    $[f(1)]^2 > [f(-1)]^2$ 
	    $|f(1)|^2 > |f(-1)|^2$ 
	    $|f(1)| > |f(-1)|$   (C) 
- 解法2：排除法 ( #特例法) #反例 
	- 例子是$f(x)=e^x$ 
		- $f'(x)=e^x$，所以$f(x)f'(x)=(e^x)(e^x)=e^{2x}$。因为$e^{2x}$永远大于0
	- $f(1)=e$， $f(-1)=e^{-1}=\frac{1}{e}$。
	    *   检验选项：
	        (A) $f(1)>f(-1)$？ $e > \frac{1}{e}$，成立。
	        (B) $f(1)<f(-1)$？ $e < \frac{1}{e}$，不成立。**排除(B)**。
	        (C) $|f(1)|>|f(-1)|$？ $|e| > |\frac{1}{e}|$，成立。
	        (D) $|f(1)|<|f(-1)|$？ $|e| < |\frac{1}{e}|$，不成立。**排除(D)**。
	- 例子二 是 $f(x)=-e^x$ 
		- (A) $f(1)>f(-1)$？ $-e > -\frac{1}{e}$，这等价于$e < \frac{1}{e}$，显然不成立。**排除(A)** 
	
-  衍生  
	- 表达式变化 
		- 如果题目条件变为$\frac{f'(x)}{f(x)}>0$ (假设$f(x) \neq 0$)，结论是什么？ 
			- 我们知道$[\ln|f(x)|]' = \frac{f'(x)}{f(x)}$。条件意味着$\ln|f(x)|$是单调递增的。因为对数函数$y=\ln u$本身是增函数，所以其真数$|f(x)|$也必须是单调递增的。因此，我们同样可以得到$|f(1)|>|f(-1)|$ 
	- 如果题目条件变为$xf'(x)+f(x)>0$，结论是什么？
	    *   **解析**：观察左侧表达式，它正好是乘积求导法则的形式，即$[x f(x)]' = x'f(x) + xf'(x) = f(x)+xf'(x)$。所以条件意味着函数$g(x)=xf(x)$的导数$g'(x)>0$。因此$g(x)$是单调递增的。我们可以得到$g(1)>g(-1)$，即$1 \cdot f(1) > -1 \cdot f(-1)$，也就是$f(1)>-f(-1)$或$f(1)+f(-1)>0$


![[Pasted image 20251004021721.png]]
[[Pasted image 20251004022124.png]]


[[方向导数的定义]]  
[[方向导数与偏导数的关系]] 
1. 第一步：计算函数在指定点的梯度 $\nabla f(1,2,0)$ 
	*   对 $x$ 求偏导：$\frac{\partial f}{\partial x} = \frac{\partial}{\partial x}(x^2y + z^2) = 2xy$
    *   对 $y$ 求偏导：$\frac{\partial f}{\partial y} = \frac{\partial}{\partial y}(x^2y + z^2) = x^2$
    *   对 $z$ 求偏导：$\frac{\partial f}{\partial z} = \frac{\partial}{\partial z}(x^2y + z^2) = 2z$
2. 将点 $P_0(1,2,0)$ 的坐标代入上述偏导数表达式，得到梯度向量在这一点的具体值：
    *   $\left. \frac{\partial f}{\partial x} \right|_{(1,2,0)} = 2(1)(2) = 4$
    *   $\left. \frac{\partial f}{\partial y} \right|_{(1,2,0)} = (1)^2 = 1$
    *   $\left. \frac{\partial f}{\partial z} \right|_{(1,2,0)} = 2(0) = 0$ 
3. 函数在点 $(1,2,0)$ 的梯度为：
    $\nabla f(1,2,0) = (4, 1, 0)$ 
4. 第二步：将 #方向向量单位化  
	1. 给定的方向向量是 $\mathbf{n} = (1,2,2)$。我们需要计算它的模（长度）：
	    $|\mathbf{n}| = \sqrt{1^2 + 2^2 + 2^2} = \sqrt{1 + 4 + 4} = \sqrt{9} = 3$ 
	2. 用原向量除以它的模，得到同方向的 #单位向量 $\mathbf{u}$：
	    $\mathbf{u} = \frac{\mathbf{n}}{|\mathbf{n}|} = \frac{1}{3}(1,2,2) = (\frac{1}{3}, \frac{2}{3}, \frac{2}{3})$ 
5. 第三步：计算梯度与单位方向向量的点积 
	1. 将第一步和第二步的结果进行 #点积运算 ，这就是所求的 #方向导数 ：
	    $\left. \frac{\partial f}{\partial \mathbf{n}} \right|_{(1,2,0)} = D_{\mathbf{u}}f(1,2,0) = \nabla f(1,2,0) \cdot \mathbf{u}$
	    $= (4, 1, 0) \cdot (\frac{1}{3}, \frac{2}{3}, \frac{2}{3})$
	    $= 4 \times \frac{1}{3} + 1 \times \frac{2}{3} + 0 \times \frac{2}{3}$
	    $= \frac{4}{3} + \frac{2}{3} + 0 = \frac{6}{3} = 2$   (D)
	
- 衍生 
	- #梯度的几何意义 [[梯度的几何意义]]
	- [[方向导数为0]] 
	- #梯度与链式法则的结合 
		- 如果一个质点沿着曲线 $\mathbf{r}(t) = (x(t), y(t), z(t))$ 运动，而它所处的空间中有一个场函数（如温度场、电势场）$f(x,y,z)$，那么函数值 $f$ 随时间 $t$ 的变化率可以通过链式法则计算：
	        $\frac{df}{dt} = \frac{\partial f}{\partial x}\frac{dx}{dt} + \frac{\partial f}{\partial y}\frac{dy}{dt} + \frac{\partial f}{\partial z}\frac{dz}{dt} = \nabla f \cdot \mathbf{r}'(t)$
	    *   这本质上是方向导数概念的延伸，方向是质点的速度向量 $\mathbf{r}'(t)$。

![[Pasted image 20251004021727.png]]
[[Pasted image 20251004022133.png]]
[[Pasted image 20251004022144.png]]

 C  


![[Pasted image 20251004021732.png]]
![[Pasted image 20251004022156.png]]
这道题的解法核心是利用**矩阵的特征值**与**矩阵的可逆性**之间的关系。  
#单位列向量      #较复杂  
#单位化的特征向量作为列向量   [[单位列向量单位行向量]]  [[矩阵乘法的运算法则]]  
*   矩阵 $A$ **可逆** $\iff$ $\det(A) \neq 0$ $\iff$ $A$ 的所有特征值**均不为零**。
*   矩阵 $A$ **不可逆**（或称奇异的） $\iff$ $\det(A) = 0$ $\iff$ $A$ **至少有一个特征值为零**。 
#矩阵可逆性与特征值的关系   [[矩阵可逆性与特征值的关系]]  
- 矩阵 $\alpha\alpha^T$ 的性质与特征值 
	1.  $\alpha$ 是一个 $n$ 维列向量，即一个 $n \times 1$ 的矩阵。  
	2.  $\alpha$ 是一个**单位向量**，这意味着它的模长为1。在线性代数中，这表示 $\alpha^T\alpha = 1$。
1.  $A = \alpha\alpha^T$。
	*   $\alpha$ 是 $n \times 1$ 矩阵，$\alpha^T$ 是 $1 \times n$ 矩阵。
	*   因此，$A = \alpha\alpha^T$ 是一个 $n \times n$ 的方阵。
	*   这个矩阵的**秩**为1
		**结论**：矩阵 $\alpha\alpha^T$ 的特征值为：一个 $1$ 和 $n-1$ 个 $0$。
2. 矩阵 $kE+cA$ 的特征值 
	设矩阵 $A$ 的特征值为 $\lambda$，对应的特征向量为 $x$，即 $Ax = \lambda x$。
	现在我们来求矩阵 $B = kE+cA$ 的特征值，其中 $k$ 和 $c$ 是常数。
	$Bx = (kE+cA)x = kEx + cAx = kx + c(\lambda x) = (k+c\lambda)x$
	这表明，如果 $\lambda$ 是 $A$ 的特征值，那么 $(k+c\lambda)$ 就是矩阵 $kE+cA$ 的特征值
- 综合求解 
	已经知道 $\alpha\alpha^T$ 的特征值为 $\lambda_1=1$ 和 $\lambda_2, \dots, \lambda_n = 0$
1. (A) $E-\alpha\alpha^T$ 
	这个矩阵的形式是 $1 \cdot E + (-1) \cdot (\alpha\alpha^T)$。
	它的特征值为 $1 + (-1)\lambda = 1-\lambda$。
	*   对应 $\lambda=1$，特征值为 $1-1=0$。
	*   对应 $\lambda=0$，特征值为 $1-0=1$（有 $n-1$ 个）。
	所以，矩阵 $E-\alpha\alpha^T$ 的特征值包含一个 $0$。根据知识点1，该矩阵**不可逆**。
2. (B) $E+\alpha\alpha^T$ 
	这个矩阵的形式是 $1 \cdot E + 1 \cdot (\alpha\alpha^T)$。
    它的特征值为 $1 + 1\lambda = 1+\lambda$。
    *   对应 $\lambda=1$，特征值为 $1+1=2$。
    *   对应 $\lambda=0$，特征值为 $1+0=1$（有 $n-1$ 个）。
    所有特征值（2和1）都非零，因此该矩阵**可逆**。
3. (C) $E+2\alpha\alpha^T$
    这个矩阵的形式是 $1 \cdot E + 2 \cdot (\alpha\alpha^T)$。
    它的特征值为 $1+2\lambda$。
    *   对应 $\lambda=1$，特征值为 $1+2(1)=3$。
    *   对应 $\lambda=0$，特征值为 $1+2(0)=1$（有 $n-1$ 个）。
    所有特征值（3和1）都非零，因此该矩阵**可逆**。
4. (D) $E-2\alpha\alpha^T$
    这个矩阵的形式是 $1 \cdot E + (-2) \cdot (\alpha\alpha^T)$。
    它的特征值为 $1-2\lambda$。
    *   对应 $\lambda=1$，特征值为 $1-2(1)=-1$。
    *   对应 $\lambda=0$，特征值为 $1-2(0)=1$（有 $n-1$ 个）。
    所有特征值（-1和1）都非零，因此该矩阵**可逆**。
**最终答案：** 只有选项 (A) 的矩阵 $E-\alpha\alpha^T$ 有一个零特征值，因此它是不可逆的。

- 衍生 
	- 非单位向量的情况 
		- 如果题目没有说 $\alpha$ 是单位向量，而是任意非零列向量。设 $\alpha^T\alpha = k$ ($k>0$)。
		    那么 $(\alpha\alpha^T)\alpha = \alpha(\alpha^T\alpha) = k\alpha$。
		    此时 $\alpha\alpha^T$ 的非零特征值为 $k$，其余 $n-1$ 个特征值仍为 $0$。
		    在这种情况下，矩阵 $E-c\alpha\alpha^T$ 的特征值为 $1-ck$ 和 $1$。该矩阵不可逆的条件是 $1-ck=0$，即 $c = 1/k = 1/(\alpha^T\alpha)$。这是一个非常经典的变形考点。
	- 行列式的计算 
		- 考察形如 $\det(E+c\alpha\alpha^T)$ 的行列式的值。
		    因为行列式是所有特征值的乘积，所以：
		    $\det(E+c\alpha\alpha^T) = (1+c \cdot 1) \cdot (1+c \cdot 0)^{n-1} = 1+c$
		    （这里假设了 $\alpha$ 是单位向量）。
		    例如，$\det(E-\alpha\alpha^T) = 1-1=0$，$\det(E+2\alpha\alpha^T) = 1+2=3$。
		    这也可以通过 #矩阵行列式引理 
		    来计算：$\det(A+uv^T) = \det(A)(1+v^TA^{-1}u)$。当 $A=E$ 时，公式简化为 $\det(E+uv^T) = 1+v^Tu$
		    对于 $\det(E+c\alpha\alpha^T)$，令 $u=c\alpha, v=\alpha$，则 $\det(E+c\alpha\alpha^T) = 1+\alpha^T(c\alpha) = 1+c(\alpha^T\alpha) = 1+c$
		 [[行列式的性质]]
	- [[投影矩阵的几何意义]] 
		- 当 $\alpha$ 是单位向量时，矩阵 $P = \alpha\alpha^T$ 是一个**投影矩阵**，它将任意向量 $x$ 投影到由向量 $\alpha$ 张成的线性空间（一条直线）上
	- #矩阵的迹 
		- 矩阵的迹等于其对角线元素之和，也等于其所有特征值之和。
		    $\text{tr}(\alpha\alpha^T) = \sum_{i=1}^n \lambda_i = 1 + 0 + \dots + 0 = 1$。
		    同时，$\text{tr}(\alpha\alpha^T) = \text{tr}(\alpha^T\alpha) = \alpha^T\alpha = 1$。（注意：$\text{tr}(AB)=\text{tr}(BA)$）
			这可以作为验证特征值之和的快速方法
		

![[Pasted image 20251004021738.png]]
[[Pasted image 20251004022202.png]]


[[矩阵可对角化的条件]]  
1. 第一步：检查 #相似的必要条件  ——计算特征值 [[矩阵相似的定义，性质]]   
	1. 上三角（或下三角）矩阵的特征值就是其主对角线上的元素。 #三角矩阵的特征值   
	    *   $A$ 的特征值为 $\lambda_1=2, \lambda_2=2, \lambda_3=1$。
	    *   $B$ 的特征值为 $\lambda_1=2, \lambda_2=2, \lambda_3=1$。
	    *  $C$ 的特征值为 $\lambda_1=1, \lambda_2=2, \lambda_3=2$
	2. 三个矩阵具有完全相同的特征值集合 $\{1, 2, 2\}$。因此，它们之间**有可能**相似。我们还需要进一步判断。
2. 第二步：判断 A、B 是否与对角矩阵 C 相似 
	1. 一个矩阵与对角矩阵 C 相似，等价于这个矩阵是  #可对角化 的  
		1. 问题就转化为判断矩阵 A 和 B 是否可对角化  
			1. 只需检查重根特征值的几何重数是否等于其代数重数 
	*   对于特征值 $\lambda=1$，它是一个单根，代数重数为1，其几何重数必然也为1。所以我们无需检查它。
	*   我们需要检查的是重根特征值 $\lambda=2$。它的**代数重数是 2**。 

3. 检验矩阵 A 
	1. $2E - A = 2\begin{pmatrix} 1 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & 1 \end{pmatrix} - \begin{pmatrix} 2 & 0 & 0 \\ 0 & 2 & 1 \\ 0 & 0 & 1 \end{pmatrix} = \begin{pmatrix} 0 & 0 & 0 \\ 0 & 0 & -1 \\ 0 & 0 & 1 \end{pmatrix}$ 
		1. 这个矩阵的秩 $r(2E - A)$ 很明显是 1 (因为第二行和第三行成比例，且存在非零行)。
			1. $\lambda=2$ 的几何重数为 $3 - r(2E - A) = 3 - 1 = 2$。
		2. 矩阵 A 满足可对角化的充要条件，因此 A 是可对角化的。它相似于一个对角矩阵，该对角矩阵的对角元是其特征值 1, 2, 2。这个对角矩阵就是 C（对角元顺序不影响相似性）。所以，**A 与 C 相似**。 
4. 检验矩阵 B 
	1. $2E - B = 2\begin{pmatrix} 1 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & 1 \end{pmatrix} - \begin{pmatrix} 2 & 1 & 0 \\ 0 & 2 & 0 \\ 0 & 0 & 1 \end{pmatrix} = \begin{pmatrix} 0 & -1 & 0 \\ 0 & 0 & 0 \\ 0 & 0 & 1 \end{pmatrix}$ 
		1. 这个矩阵的秩 $r(2E - B)$ 很明显是 2 
			1. $\lambda=2$ 的几何重数为 $3 - r(2E - B) = 3 - 2 = 1$
		2. 几何重数 (1) < 代数重数 (2)。
			**结论**: 矩阵 B 不满足可对角化的充要条件，因此 B 是**不可对角化的**。所以，**B 与 C 不相似**。

- 衍生 
	- #相似矩阵的性质 
		- 若 $A \sim B$，则下列说法正确的是？
	    *   你需要记住相似矩阵具有相同的：
	        *   特征多项式和特征值 (包括重数)
	        *   行列式 ($|A|=|B|$)
	        *   迹 ($tr(A)=tr(B)$)
	        *   秩 ($r(A)=r(B)$)
	    *   注意：**拥有相同的特征值、行列式、迹、秩并不能保证两个矩阵相似**。本题就是一个绝佳的反例（A 和 B 具有所有这些相同性质，但它们不相似，因为一个可对角化，另一个不可）。
	- 矩阵可对角化的其他方法 
		*   **充分条件1**: $n$ 阶矩阵若有 $n$ 个**不同**的特征值，则它一定可对角化。
	    *   **充分条件2**: **实对称矩阵**一定可以相似对角化。
	* 求 #相似变换矩阵 P 
		*  如果矩阵 A 可对角化，题目可能会要求你找到一个可逆矩阵 $P$，使得 $P^{-1}AP = D$ (D 为对角矩阵)。
	    *   求解步骤：
	        1.  求出 A 的所有特征值 $\lambda_1, \lambda_2, ..., \lambda_n$。
	        2.  对每个特征值 $\lambda_i$，求出其对应的线性无关的特征向量。
	        3.  将这些特征向量作为列向量，构成矩阵 $P$。
	        4.  此时 $P$ 就是所求的矩阵，且 $D$ 是以对应特征值为对角元的对角矩阵。
	- #若尔当标准型  
	    *   这是对角化概念的延伸。对于**不可对角化**的矩阵（如本题中的 B），它虽然不能相似于一个对角矩阵，但一定能相似于一个**若尔当标准型**矩阵。
	    *   矩阵 B 的若尔当标准型就是它自身：$J_B = \begin{pmatrix} 2 & 1 & 0 \\ 0 & 2 & 0 \\ 0 & 0 & 1 \end{pmatrix}$。
	    *   两个矩阵相似的**充要条件**是它们具有相同的若尔当标准型（不计若尔当块的排列顺序）。
	- #矩阵对角化的应用 
		-  #计算矩阵的高次幂 : 如果 $A = PDP^{-1}$，那么 $A^k = PD^kP^{-1}$。计算对角矩阵 $D$ 的幂次非常简单，只需将对角元各自幂次即可。
	    *   **求解线性常系数微分方程组**。





![[Pasted image 20251004021745.png]]
[[Pasted image 20251004022212.png]]

#随机事件  #条件概率    [[条件概率]] 
- 解法1：公式推导法 (根本方法) 
	- 第一步：展开原始条件
	-  $P(A|B) > P(A|\bar{B})$。
		根据 #条件概率的定义 ，$P(X|Y) = \frac{P(XY)}{P(Y)}$，我们对不等式两边进行展开：
		左边：$P(A|B) = \frac{P(AB)}{P(B)}$
		右边：$P(A|\bar{B}) = \frac{P(A\bar{B})}{P(\bar{B})}$
		
		所以，原不等式等价于：

	- 第二步：替换概率项 
		- 使用概率的基本公式进行替换：
		$P(A\bar{B}) = P(A) - P(AB)$ (事件A发生，可以分为A和B同时发生，以及A发生但B不发生两种情况，即 $P(A) = P(AB) + P(A\bar{B})$)
		$P(\bar{B}) = 1 - P(B)$
		- $\frac{P(AB)}{P(B)} > \frac{P(A)-P(AB)}{1-P(B)}$ 
	- 第三步：化简不等式 
		- 题目条件 $0<P(B)<1$，所以 $P(B)>0$ 且 $1-P(B)>0$。我们可以将分母 $P(B)$ 和 $1-P(B)$ 乘到不等式两边，且不等号方向不变 
		- $P(AB)(1-P(B)) > P(B)(P(A)-P(AB))$ 
		- 展开括号：
			$P(AB) - P(AB)P(B) > P(A)P(B) - P(B)P(AB)$
		 - $P(AB) > P(A)P(B)$ 
	 - 第四步：建立等价关系
		 - $P(A|B) > P(A|\bar{B}) \iff P(AB) > P(A)P(B)$ 
			 - 这个中间结论非常重要，它表明“B的发生对A有促进作用”等价于“A和B正相关”
	 - 第五步：分析选项
		 - 我们用同样的方法来分析选项(A)：$P(B|A) > P(B|\bar{A})$
			将A和B的角色互换，完全按照上面的步骤进行推导：
			$P(B|A) > P(B|\bar{A})$
			$\iff \frac{P(BA)}{P(A)} > \frac{P(B\bar{A})}{P(\bar{A})}$
			$\iff \frac{P(AB)}{P(A)} > \frac{P(B)-P(AB)}{1-P(A)}$
			$\iff P(AB)(1-P(A)) > P(A)(P(B)-P(AB))$
			$\iff P(AB) - P(AB)P(A) > P(A)P(B) - P(A)P(AB)$
			$\iff P(AB) > P(A)P(B)$
	- 我们发现，题目给出的条件 $P(A|B) > P(A|\bar{B})$ 和选项(A) $P(B|A) > P(B|\bar{A})$ 都等价于 $P(AB) > P(A)P(B)$。 因此，它们之间互为充分必要条件。所以正确答案是(A)

-  解法2：特殊情况法 (应试技巧) 
	- 第一步：构造一个满足条件的特殊情况 
		- 最简单的正相关例子：令 $A=B$
			因为 $0<P(A)<1$，所以 $0<P(B)<1$ 也满足。
			我们来验证此时原条件 $P(A|B) > P(A|\bar{B})$ 是否成立：
			$P(A|B) = P(A|A) = 1$
			$P(A|\bar{B}) = P(A|\bar{A}) = 0$
			显然，$1 > 0$，所以原条件成立
	- 第二步：检验各个选项
		- 作为其充分必要条件的正确选项，在 $A=B$ 这个特殊情况下也必须成立。
			*   **对于(A):** $P(B|A) > P(B|\bar{A})$
			    $P(B|A) = P(A|A) = 1$
			    $P(B|\bar{A}) = P(A|\bar{A}) = 0$
			    $1 > 0$ 成立。所以(A)可能是正确答案。
			
			*   **对于(B):** $P(B|A) < P(B|\bar{A})$
			    $1 < 0$ 不成立。所以(B)被排除。
			
			*   **对于(C):** $P(\bar{B}|A) > P(\bar{B}|\bar{A})$
			    $P(\bar{B}|A) = P(\bar{A}|A) = 0$
			    $P(\bar{B}|\bar{A}) = P(\bar{A}|\bar{A}) = 1$
			    $0 > 1$ 不成立。所以(C)被排除。

			






![[Pasted image 20251004021752.png]]
[[Pasted image 20251004022220.png]]
[[Pasted image 20251004022227.png]]

#简单随机样本   
#卡方分布   [[卡方分布]] 
[[抽样分布的基本性质（样本均值）]]  
	



![[Pasted image 20251004021757.png]]
[[Pasted image 20251004022235.png]]


#高阶导数对低阶导数的关系    
#奇偶性求高阶导数 
我们已经得出 $f'''(x)$ 是一个奇函数。根据奇函数的定义，我们有 $f'''(-x) = -f'''(x)$。
对于一个在 $x=0$ 处有定义的奇函数，我们可以令 $x=0$，得到：
$f'''(-0) = -f'''(0)$
$f'''(0) = -f'''(0)$
$2f'''(0) = 0$
$f'''(0) = 0$

因此，最终答案是 **0**。






![[Pasted image 20251004021807.png]]
[[Pasted image 20251004022246.png]]

[[二阶常系数齐次线性微分方程的求解]] 

$r^2+2r+3=0$


计算判别式 $\Delta$：
$\Delta = b^2-4ac = 2^2-4(1)(3) = 4-12 = -8$

判别式 $\Delta < 0$，所以该方程有一对共轭复数根
$r = \frac{-2\pm\sqrt{-8}}{2(1)} = \frac{-2\pm\sqrt{8}i}{2} = \frac{-2\pm2\sqrt{2}i}{2}$

分离实部和虚部，得到两个根：
$r_{1,2} = -1\pm\sqrt{2}i$

根据特征方程根的不同情况，通解有三种不同的形式：

1.  **两个不相等的实根** $r_1, r_2$：通解为 $y=C_1e^{r_1x}+C_2e^{r_2x}$。
2.  **两个相等的实根** $r_1=r_2=r$：通解为 $y=(C_1+C_2x)e^{rx}$。
3.  **一对共轭复数根** $r=\alpha\pm\beta i$：通解为 $y=e^{\alpha x}(C_1\cos(\beta x)+C_2\sin(\beta x))$。

将 $\alpha$ 和 $\beta$ 的值代入通解公式 $y=e^{\alpha x}(C_1\cos(\beta x)+C_2\sin(\beta x))$，我们得到：
$y=e^{-1x}(C_1\cos(\sqrt{2}x)+C_2\sin(\sqrt{2}x))$
 
即最终的通解为：
$y=e^{-x}(C_1\cos(\sqrt{2}x)+C_2\sin(\sqrt{2}x))$

- 衍生 
	- #二阶常系数非齐次线性微分方程  
		方程右边不为 $0$，形如 $ay''+by'+cy=f(x)$。其通解结构为 $y=y_c+y_p$，其中：
	    *   $y_c$ 是对应的齐次方程 $ay''+by'+cy=0$ 的通解（也就是我们本题求解的部分）。
	    *   $y_p$ 是非齐次方程的一个特解，通常使用 #待定系数法 来求。这需要根据右侧函数 $f(x)$ 的形式（如多项式、指数函数、三角函数或它们的组合）来假设特解的形式。








![[Pasted image 20251004021812.png]]
[[Pasted image 20251004022253.png]]

[[第二类曲线积分与路径无关的条件]] 

1. 标准化积分形式
	分解为 $\int_L \frac{x}{x^2 + y^2 - 1}dx + \frac{-ay}{x^2 + y^2 - 1}dy$ 
    由此，我们可以确定 $P(x,y)$ 和 $Q(x,y)$:
    $P(x,y) = \frac{x}{x^2 + y^2 - 1}$
    $Q(x,y) = \frac{-ay}{x^2 + y^2 - 1}$
2. 应用路径无关的条件 
	1. 对于一个形如 $\int_L P(x,y)dx + Q(x,y)dy$ 的曲线积分，如果它在某个 #单连通区域 内与 #路径无关 ，那么其 #充要条件  是 $\frac{\partial P}{\partial y} = \frac{\partial Q}{\partial x}$。
	来检查一下题目给定的区域 $D = \{(x,y) | x^2 + y^2 < 1\}$ 
		这是一个以原点为中心、半径为1的开圆盘。开圆盘内部没有“洞”，是典型的 #单连通区域 
3. 计算偏导数 
	1. $\frac{\partial P}{\partial y} = \frac{-2xy}{(x^2 + y^2 - 1)^2}$
	2. $\frac{\partial Q}{\partial x} = \frac{2axy}{(x^2 + y^2 - 1)^2}$ 
4. 根据路径无关的条件，令两个偏导数相等：
	    $\frac{\partial Q}{\partial x} = \frac{\partial P}{\partial y}$
	    $\frac{2axy}{(x^2 + y^2 - 1)^2} = \frac{-2xy}{(x^2 + y^2 - 1)^2}$
	只要 $x$ 和 $y$ 不全为0，我们就可以消去等式两边的公因子 $\frac{2xy}{(x^2 + y^2 - 1)^2}$  
	  $a = -1$ 

-  衍生
	- #求解势函数 
		- 如果确定了积分与路径无关，题目可能会进一步要求你找出其势函数 $u(x,y)$。
		    以本题为例，当 $a=-1$ 时，我们有 $P = \frac{x}{x^2+y^2-1}$ 和 $Q = \frac{y}{x^2+y^2-1}$。
		    求解 $u(x,y)$ 使得 $\frac{\partial u}{\partial x} = P$ 且 $\frac{\partial u}{\partial y} = Q$。
		    *   对 $P$ 关于 $x$ 积分:
		        $u(x,y) = \int \frac{x}{x^2+y^2-1} dx = \frac{1}{2} \int \frac{d(x^2+y^2-1)}{x^2+y^2-1} = \frac{1}{2}\ln|x^2+y^2-1| + C(y)$
		    *   对结果关于 $y$ 求导，并令其等于 $Q$:
		        $\frac{\partial u}{\partial y} = \frac{1}{2} \frac{2y}{x^2+y^2-1} + C'(y) = \frac{y}{x^2+y^2-1} + C'(y)$
		    *   令 $\frac{\partial u}{\partial y} = Q(x,y) = \frac{y}{x^2+y^2-1}$，可得 $C'(y)=0$，所以 $C(y)$ 是一个常数 $C$。
		    *   因此，势函数为 $u(x,y) = \frac{1}{2}\ln|x^2+y^2-1| + C$。
	- #非单连通区域下的路径无关问题 最经典的衍生考点。如果区域 $D$ 不是单连通的（例如，挖掉一个点的平面 $R^2 \setminus \{(0,0)\}$），那么 $\frac{\partial P}{\partial y} = \frac{\partial Q}{\partial x}$ 仅仅是路径无关的**必要条件**，而非充分条件。
		    **经典例子:**
		    考虑积分 $\oint_L \frac{-ydx + xdy}{x^2+y^2}$，其中 $L$ 是绕原点的单位圆。
		    这里 $P = \frac{-y}{x^2+y^2}$, $Q = \frac{x}{x^2+y^2}$。
		    可以验证 $\frac{\partial P}{\partial y} = \frac{y^2-x^2}{(x^2+y^2)^2}$ 并且 $\frac{\partial Q}{\partial x} = \frac{y^2-x^2}{(x^2+y^2)^2}$。
		    两者相等！但是，如果计算沿单位圆的积分，结果是 $2\pi$，不为0。这说明积分与路径有关。原因就在于积分区域包含了一个“奇点”(0,0)，使得区域非单连通。
	- #利用势函数计算曲线积分   
		一旦求出了势函数 $u(x,y)$，计算从点 $A(x_1, y_1)$ 到点 $B(x_2, y_2)$ 的曲线积分就变得非常简单，这被称为 #牛顿-莱布尼茨公式在曲线积分上的推广  [[牛顿莱布尼茨公式]]  
	    $\int_A^B Pdx + Qdy = u(B) - u(A) = u(x_2, y_2) - u(x_1, y_1)$
	    这比参数化曲线进行计算要简便得多。


![[Pasted image 20251004021821.png]]
[[Pasted image 20251004022300.png]]


[[调和函数的定义与性质]] 
#幂级数的和函数   
#和函数  
[[数列求和公式]]  
[[逐项积分法，无穷级数，幂级数的求和性质，幂级数的逐项求导]] 
1. 步骤 1：构造一个更简单的相关级数  
	1. 观察题目给出的级数 $\sum_{n=1}^{\infty} (-1)^{n-1}nx^{n-1}$，我们可以发现它的每一项 $nx^{n-1}$ 恰好是 $x^n$ 的导数 
2. #逐项积分法 指的是，当有一个函数级数（比如幂级数）时，**对这个级数的和函数进行积分，等价于先对级数中的每一项函数单独进行积分，然后再把积分后的结果求和**。
	1. 用数学语言表达就是：
		如果 $S(x) = \sum_{n=0}^{\infty} f_n(x)$，那么逐项积分法就是指下面的等式成立：
		$\int_a^b S(x) dx = \int_a^b \left( \sum_{n=0}^{\infty} f_n(x) \right) dx = \sum_{n=0}^{\infty} \left( \int_a^b f_n(x) dx \right)$
		**核心操作**：就是**交换积分符号 $\int$ 和求和符号 $\sum$ 的顺序**。
		设 $F(x) = \int S(x) dx$。根据幂级数的性质，在收敛域内，积分和求和的顺序可以交换：
		$F(x) = \int \left[ \sum_{n=1}^{\infty} (-1)^{n-1}nx^{n-1} \right] dx = \sum_{n=1}^{\infty} \int (-1)^{n-1}nx^{n-1} dx$
	2. 计算积分，我们得到：
		$F(x) = \sum_{n=1}^{\infty} (-1)^{n-1}x^n + C$ (其中 C 是积分常数) 
3. 问题转化为了求级数 $\sum_{n=1}^{\infty} (-1)^{n-1}x^n$ 的和
	1. 这个新的级数是一个非常标准的 **几何级数**（Geometric Series），也称等比级数
		1. [[几何级数的求和公式]]  
		2. $\sum_{n=1}^{\infty} (-1)^{n-1}x^n = x - x^2 + x^3 - x^4 + \dots$ 
		3. #几何级数的求和公式 
			$S(x) = \sum_{k=0}^{\infty} x^k = \frac{1}{1-x}$ (当 $|x|<1$ 时)
			$\sum_{n=0}^{\infty} ar^n$=$\frac{a}{1-r}$ $r< 1$
		4. 该级数的和为：
			$\sum_{n=1}^{\infty} (-1)^{n-1}x^n = \frac{x}{1 - (-x)} = \frac{x}{1+x}$
4. 步骤 3：通过求导得到原级数的和函数 $S(x)$ 
	1. $S(x)$ 是 $F(x)$ 的导数，即 $S(x) = F'(x)$。
		我们已经求出 $F(x) = \frac{x}{1+x} + C$ 
	2. 现在对 $F(x)$ 求导：
		$S(x) = \frac{d}{dx} \left( \frac{x}{1+x} + C \right)$
	3. #除法法则   
		1.  $(\frac{u}{v})' = \frac{u'v - uv'}{v^2}$
			令 $u=x$, $v=1+x$，则 $u'=1$, $v'=1$。
			$S(x) = \frac{1 \cdot (1+x) - x \cdot 1}{(1+x)^2} + 0 = \frac{1+x-x}{(1+x)^2} = \frac{1}{(1+x)^2}$ 
	4. 因此，幂级数 $\sum_{n=1}^{\infty} (-1)^{n-1}nx^{n-1}$ 在区间 $(-1,1)$ 内的和函数为 $S(x) = \frac{1}{(1+x)^2}$ 

- 衍生 
	-  通过 #逐项积分 求解 
		- 求和函数 $S(x) = \sum_{n=1}^{\infty} \frac{x^n}{n}$ 在 $|x|<1$ 上的和
			-  **思路:** 看到分母是 $n$，想到它是 $x^n$ 积分的结果。
		*   **解法:** 
		    *   先对 $S(x)$ #逐项求导 ：$S'(x) = \sum_{n=1}^{\infty} \frac{nx^{n-1}}{n} = \sum_{n=1}^{\infty} x^{n-1} = 1 + x + x^2 + \dots$
		    *   这是一个公比为 $x$ 的几何级数，和为 $S'(x) = \frac{1}{1-x}$。
		    *   再对结果积分：$S(x) = \int \frac{1}{1-x} dx = -\ln(1-x) + C$。
		    *   通过代入 $x=0$ 来确定常数 C：$S(0) = \sum_{n=1}^{\infty} \frac{0^n}{n} = 0$，而 $-\ln(1-0) + C = C$，所以 $C=0$。
		    *   最终得到 $S(x) = -\ln(1-x)$。
	- 需要多次求导或积分
		例如，求和函数 $S(x) = \sum_{n=1}^{\infty} n^2x^{n-1}$。
		*   **思路:** 出现 $n^2$ 意味着可能需要两次求导/积分操作。
		*   **解法:**
		    *   先逐项积分一次：$\int S(x) dx = \sum n x^n + C_1$。
		    *   令 $G(x) = \sum n x^n = x\sum n x^{n-1}$。我们已经从原题知道 $\sum n x^{n-1}$ 这类级数的求和方法。
		    *   $\sum_{n=1}^{\infty} nx^{n-1}$ 是 $\sum_{n=1}^{\infty} x^n$ 的导数，而 $\sum_{n=1}^{\infty} x^n = \frac{x}{1-x}$。
		    *   所以 $\sum_{n=1}^{\infty} nx^{n-1} = (\frac{x}{1-x})' = \frac{1}{(1-x)^2}$。
		    *   因此 $G(x) = \frac{x}{(1-x)^2}$。
		    *   最后 $S(x) = G'(x) = (\frac{x}{(1-x)^2})' = \frac{1+x}{(1-x)^3}$。
	- 求级数在收敛区间端点的值 ([[端点验证（阿贝尔定理）]]  
		- 如果题目要求求级数在端点处的值（例如 $x=1$ 或 $x=-1$），且级数在该点收敛，则可以使用阿贝尔定理，即和函数在端点处的极限值等于级数在端点处的和。
			*   **例如:** 对于上面求出的 $\sum_{n=1}^{\infty} \frac{x^n}{n} = -\ln(1-x)$。当 $x=-1$ 时，级数变为 $\sum_{n=1}^{\infty} \frac{(-1)^n}{n}$，这是一个收敛的交错级数。根据阿贝尔定理，它的和是 $\lim_{x \to -1^+} (-\ln(1-x)) = -\ln(2)$

![[Pasted image 20251004021829.png]]
[[Pasted image 20251004022309.png]]

[[向量组线性相关的充要条件]]  


#列向量组线性无关 
#向量组线性无关的性质  
1. 第一步：问题转化 
	1. 目标是求向量组 $Aα_1, Aα_2, Aα_3$ 的秩 
		1. 一个向量组的秩等于由这个向量组构成的矩阵的秩。所以，我们需要求矩阵 $C = (Aα_1, Aα_2, Aα_3)$ 的秩，即 $r(C)$
	2. #矩阵乘法的性质 ，我们可以将矩阵 $C$ 进行改写：
		$C = (Aα_1, Aα_2, Aα_3) = A(α_1, α_2, α_3)$   [[矩阵乘法的运算法则]]
	3. 令 $B = (α_1, α_2, α_3)$，那么问题就转化为了求矩阵乘积 $AB$ 的秩，即 $r(AB)$ 
2. 第二步：分析已知条件 
	1. $α_1, α_2, α_3$ 为线性无关的 3 维列向量组 
		1. $α_1, α_2, α_3$ 是3维向量，意味着它们有3个分量。
			*   将它们构成矩阵 $B = (α_1, α_2, α_3)$，这是一个 $3 \times 3$ 的方阵。
			*   因为这3个列向量线性无关，所以这个 $3 \times 3$ 方阵 $B$ 是**满秩**的。对于方阵来说，满秩等价于**可逆**
		2.  所以，我们得出结论：矩阵 $B$ 是一个3阶可逆矩阵，其秩 $r(B)=3$  
3. 第三步：应用矩阵   [[秩的性质]] 
	1. 与可逆矩阵相乘不改变秩**：如果矩阵 $P$ 是一个可逆矩阵，那么 $r(PA) = r(A)$ 且 $r(AP) = r(A)$ 
		1. 我们不再需要关心 $α_1, α_2, α_3$ 具体是什么，只需要求出矩阵 $A$ 的秩即可
4. 第四步：计算矩阵 A 的秩 
	1. 矩阵 $A$ 为：
		$A = \begin{pmatrix} 1 & 0 & 1 \\ 1 & 1 & 2 \\ 0 & 1 & 1 \end{pmatrix}$ 
	2. #高斯消元法   
		1. $\begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 1 \\ 0 & 0 & 0 \end{pmatrix}$  
		2. $r(A) = 2$
	3. $r(Aα_1, Aα_2, Aα_3) = r(A(α_1, α_2, α_3)) = r(AB) = r(A) = 2$
 - 衍生 
	 - 条件变化：A 可逆 
		 - 矩阵 $A$ 是一个可逆矩阵（即 $r(A)=3$），那么 $r(Aα_1, Aα_2, Aα_3) = r(A) = 3$。这意味着，一个可逆的线性变换会将一组线性无关的向量映射为另一组线性无关的向量。这在几何上表示保住了空间的维度，没有发生“降维打击”
	 - 条件变化：$α$ 向量组线性相关 
		 - 如果题目条件变为“$α_1, α_2, α_3$ 为线性相关的3维列向量组”，那么矩阵 $B=(α_1, α_2, α_3)$ 就不是可逆矩阵，其秩 $r(B) < 3$ 
		 - 我们不能再用 $r(AB)=r(A)$。我们需要使用 [[秩的性质]]  
			 **乘积的秩**：对于矩阵乘法 $AB$，有 $r(AB) \le \min(r(A), r(B))$  #秩不等式  
			 - 已经算出 $r(A)=2$，并且知道 $r(B)$ 可能是1或2，所以最终结果 $r(AB) \le \min(2, r(B))$。结果将不唯一，可能需要更多信息才能确定
	- 抽象化提问
		- 可能不给出具体的矩阵 $A$，而是直接给出它的性质。例如：
		    “设 $A$ 是一个 $3 \times 3$ 矩阵，且 $r(A)=2$。$α_1, α_2, α_3$ 是3维线性无关的列向量组，求向量组 $Aα_1, Aα_2, Aα_3$ 的秩。”
		    解题思路完全一样，直接利用 $r(A(α_1, α_2, α_3)) = r(A) = 2$ 即可
	- 线性方程组的联系 
		- $r(A)=2$ 意味着齐次线性方程组 $Ax=0$ 的解空间的维数是 $n - r(A) = 3 - 2 = 1$。
		    $r(Aα_1, Aα_2, Aα_3)=2$ 意味着向量组 $Aα_1, Aα_2, Aα_3$ 是线性相关的，存在不全为零的数 $k_1, k_2, k_3$ 使得 $k_1Aα_1 + k_2Aα_2 + k_3Aα_3 = 0$，即 $A(k_1α_1 + k_2α_2 + k_3α_3) = 0$。这说明向量 $k_1α_1 + k_2α_2 + k_3α_3$ 是方程组 $Ax=0$ 的一个非零解 
	- 从 #线性空间 和 #线性映射 的角度理解  
		- $A$ 代表一个从三维空间 $R^3$ 到自身的一个线性映射 $T$
		- $α_1, α_2, α_3$ 线性无关，说明它们是 $R^3$ 的一组基。
		    *   $r(A)=2$ 说明这个映射的 #像空间 的维数是2。像空间是由所有基向量的像 $T(α_1), T(α_2), T(α_3)$（即 $Aα_1, Aα_2, Aα_3$）张成的子空间。
		    *   因此，求向量组 $Aα_1, Aα_2, Aα_3$ 的秩，本质上就是在求这个线性映射 $T$ 的像空间的维数，而这个维数就是矩阵 $A$ 的秩。

![[Pasted image 20251004021837.png]]
[[Pasted image 20251004022317.png]]
[[Pasted image 20251004022327.png]]


[[累积分布函数的性质]]  
[[标准正态分布]]  
[[PDF与CDF的关系]] 
- 方法一：通过定义直接计算 
1. #PDF和CDF的关系  
    如果$F(x)$的导数存在且连续，那么它就是对应的概率密度函数$f(x)$。
    *   $F'(x) = \frac{d}{dx}F(x) = f(x)$
    *   $F(x) = \int_{-\infty}^{x} f(t)dt$
	1. 对已知的分布函数 $F(x)$ 求导：
		$f(x) = F'(x) = \frac{d}{dx} \left[ 0.5\Phi(x) + 0.5\Phi\left(\frac{x-4}{2}\right) \right]$
		$= 0.5 \frac{d}{dx}\Phi(x) + 0.5 \frac{d}{dx}\Phi\left(\frac{x-4}{2}\right)$
2.  $\Phi'(u) = \phi(u)$，我们得到：
	$= 0.5\phi(x) + 0.5 \cdot \phi\left(\frac{x-4}{2}\right) \cdot \frac{d}{dx}\left(\frac{x-4}{2}\right)$
	$= 0.5\phi(x) + 0.5 \cdot \phi\left(\frac{x-4}{2}\right) \cdot \frac{1}{2}$
	$= 0.5\phi(x) + 0.25\phi\left(\frac{x-4}{2}\right)$
3. 第二步：计算数学期望 $E(X)$
	1. [[期望与方差的性质和定义]]
		1. #期望的定义 
			对于一个连续型随机变量 $X$ 的函数 $g(X)$，其数学期望（或均值）定义为：
		    $E[g(X)] = \int_{-\infty}^{\infty} g(x)f(x)dx$   #期望公式 
		    其中 $f(x)$ 是 $X$ 的 PDF。积分的范围是 $X$ 的所有可能取值。
		2. 将 $f(x)$ 代入积分公式：
			$E(X) = \int_{-\infty}^{+\infty} x f(x) dx = \int_{-\infty}^{+\infty} x \left[ 0.5\phi(x) + 0.25\phi\left(\frac{x-4}{2}\right) \right] dx$ 
	2. #积分的线性性质  将其拆分为两部分：
		$E(X) = 0.5 \int_{-\infty}^{+\infty} x\phi(x)dx + 0.25 \int_{-\infty}^{+\infty} x\phi\left(\frac{x-4}{2}\right)dx$ 
4. 第三步：分别计算两个积分 
	1.  **第一个积分**：$\int_{-\infty}^{+\infty} x\phi(x)dx$。
	    这个积分本身就是标准正态分布 $N(0, 1)$ 的数学期望。我们知道标准正态分布的期望为 $0$。所以，$\int_{-\infty}^{+\infty} x\phi(x)dx = 0$
	2. **第二个积分**：$\int_{-\infty}^{+\infty} x\phi\left(\frac{x-4}{2}\right)dx$ 
		1. #换元法 令 $t = \frac{x-4}{2}$，则 $x = 2t+4$，并且 $dx = 2dt$。当 $x \to \pm\infty$ 时，$t \to \pm\infty$ 
		2. 积分变为：
		    $\int_{-\infty}^{+\infty} (2t+4)\phi(t) (2dt)$
		    $= \int_{-\infty}^{+\infty} (4t+8)\phi(t) dt$
		    $= 4\int_{-\infty}^{+\infty} t\phi(t)dt + 8\int_{-\infty}^{+\infty} \phi(t)dt$
		3. 知道 $\int_{-\infty}^{+\infty} t\phi(t)dt$ 是标准正态分布的期望，等于 $0$ 
		4. $\int_{-\infty}^{+\infty} \phi(t)dt$ 是标准正态分布密度函数在整个实数轴上的积分，其值等于 $1$（概率的归一性）。
		    所以，第二个积分为 $4 \times 0 + 8 \times 1 = 8$
5. 第四步：合并结果 
	1. $E(X) = 0.5 \times (0) + 0.25 \times (8) = 0 + 2 = 2$
- 方法二：利用 #混合分布的性质 [[混合分布]] 
	- [[全期望公式（亚当夏娃公式）]] 
1. 第一步：识别混合分布结构 
	1. 题目给出的分布函数：$F(x) = 0.5\Phi(x) + 0.5\Phi\left(\frac{x-4}{2}\right)$
		1. 可以看作是两个分布函数的加权平均，权重分别为 $p_1=0.5$ 和 $p_2=0.5$ 
		2. 意味着随机变量 $X$ 可以看作是这样一个过程产生的：以 $0.5$ 的概率从第一个分布中抽样，以 $0.5$ 的概率从第二个分布中抽样 
2. 第二步：分析两个子分布
	1. 第一个子分布
		1. 分布函数为 $F_1(x) = \Phi(x)$。
		    这是标准正态分布 $N(0, 1^2)$ 的分布函数。因此，服从该分布的随机变量 $X_1$ 的期望为 $E(X_1) = 0$。
	2. 第二个子分布
		1. 分布函数为 $F_2(x) = \Phi\left(\frac{x-4}{2}\right)$。
		    与一般正态分布 $N(\mu, \sigma^2)$ 的分布函数 $F(x) = \Phi(\frac{x-\mu}{\sigma})$ 进行比较，我们可以立刻识别出：
		    均值 $\mu = 4$
		    标准差 $\sigma = 2$
		    所以，这是一个正态分布 $N(4, 2^2)$。服从该分布的随机变量 $X_2$ 的期望为 $E(X_2) = 4$
3. 第三步：计算混合分布的期望
	1. 混合分布的期望公式：
		$E(X) = p_1 E(X_1) + p_2 E(X_2)$
		$= 0.5 \times E(X_1) + 0.5 \times E(X_2)$
		$= 0.5 \times 0 + 0.5 \times 4$
		$= 0 + 2 = 2$
- 衍生
	- #混合分布的方差  
		- 让你求 $Var(X)$。对于混合分布， #方差公式 为 $Var(X) = E(X^2) - [E(X)]^2$ 
			其中 $E(X^2) = p_1 E(X_1^2) + p_2 E(X_2^2)$。 
		-   对于 $X_1 \sim N(0, 1)$，$Var(X_1) = 1 = E(X_1^2) - [E(X_1)]^2 \Rightarrow E(X_1^2) = 1 + 0^2 = 1$。
	    *   对于 $X_2 \sim N(4, 2^2)$，$Var(X_2) = 2^2 = 4 = E(X_2^2) - [E(X_2)]^2 \Rightarrow E(X_2^2) = 4 + 4^2 = 20$。
	    *   所以 $E(X^2) = 0.5 \times 1 + 0.5 \times 20 = 0.5 + 10 = 10.5$。
	    *   最终 $Var(X) = 10.5 - 2^2 = 10.5 - 4 = 6.5$
	- 求特定区间的概率 
		- 例如，求 $P(X \le 0)$。这可以直接利用分布函数 $F(x)$ 的定义：
			- 其 #累积分布函数 $F(x)$ 的定义就是其 #概率密度函数 $f(t)$ 从负无穷到 $x$ 的积分。即 $F(x) = P(X \le x) = \int_{-\infty}^{x} f(t) dt$ 
		    $P(X \le 0) = F(0) = 0.5\Phi(0) + 0.5\Phi(\frac{0-4}{2}) = 0.5\Phi(0) + 0.5\Phi(-2)$ 
	- [[矩母函数]] 
		- 求 $X$ 的矩母函数 $M_X(t)$    #混合分布 的 #矩母函数 是其各成分矩母函数的加权平均：$M_X(t) = p_1 M_{X_1}(t) + p_2 M_{X_2}(t)$ 
			*   $X_1 \sim N(0, 1)$ 的矩母函数为 $M_{X_1}(t) = e^{t^2/2}$。
		    *   $X_2 \sim N(4, 4)$ 的矩母函数为 $M_{X_2}(t) = e^{4t + 4t^2/2} = e^{4t + 2t^2}$。
		    *   所以 $M_X(t) = 0.5e^{t^2/2} + 0.5e^{4t+2t^2}$。
		    求期望也可以通过矩母函数：$E(X) = M_X'(0)$。



![[Pasted image 20251004021843.png]]
[[Pasted image 20251004022335.png]]

[[偏导数 （隐函数求导公式）]]   [[复合函数求导]]
对于函数 $y = f(u(x), v(x))$，其对 $x$ 的一阶导数遵循 #链式法则 ：
$\frac{dy}{dx} = \frac{\partial f}{\partial u} \frac{du}{dx} + \frac{\partial f}{\partial v} \frac{dv}{dx}$
 1.  第一步：求一阶导数 $\frac{dy}{dx}$
	1. 确定中间变量及其导数
		1.  $u(x) = e^x$，所以 $\frac{du}{dx} = e^x$。
		    *   $v(x) = \cos x$，所以 $\frac{dv}{dx} = -\sin x$
	2. 应用链式法则
		1. 代入链式法则公式：
		    $\frac{dy}{dx} = \frac{\partial f}{\partial u} \cdot (e^x) + \frac{\partial f}{\partial v} \cdot (-\sin x)$
		2. 整理后得到：
		    $\frac{dy}{dx} = \frac{\partial f}{\partial u}e^x - \frac{\partial f}{\partial v}\sin x$
		    这与图片中的第一步结果完全一致
2. 第二步：求二阶导数 $\frac{d^2y}{dx^2}$    #乘法法则   #链式法则 
	1. $\frac{d^2y}{dx^2} = \frac{d}{dx} \left( \frac{\partial f}{\partial u}e^x - \frac{\partial f}{\partial v}\sin x \right) = \frac{d}{dx} \left( \frac{\partial f}{\partial u}e^x \right) - \frac{d}{dx} \left( \frac{\partial f}{\partial v}\sin x \right)$ 
	2. 分别计算两个部分： 
		1. 计算第一部分 $\frac{d}{dx} \left( \frac{\partial f}{\partial u}e^x \right)$ 
			   #乘法法则 $(ab)' = a'b + ab'$:
		    $\frac{d}{dx} \left( \frac{\partial f}{\partial u}e^x \right) = \left[ \frac{d}{dx} \left( \frac{\partial f}{\partial u} \right) \right] \cdot e^x + \frac{\partial f}{\partial u} \cdot \left[ \frac{d}{dx}(e^x) \right]$
		    *   $\frac{d}{dx}(e^x) = e^x$。
		    *   关键在于计算 $\frac{d}{dx} \left( \frac{\partial f}{\partial u} \right)$。注意，$\frac{\partial f}{\partial u}$ 本身也是一个关于 $u, v$ 的函数，所以对它求 $x$ 的导数，需要再次使用链式法则：
				$\frac{d}{dx} \left( \frac{\partial f}{\partial u} \right) = \frac{\partial}{\partial u}\left(\frac{\partial f}{\partial u}\right)\frac{du}{dx} + \frac{\partial}{\partial v}\left(\frac{\partial f}{\partial u}\right)\frac{dv}{dx} = \frac{\partial^2 f}{\partial u^2}e^x + \frac{\partial^2 f}{\partial v \partial u}(-\sin x)$
			    将这些结果代入，得到第一部分：
			    $\left( \frac{\partial^2 f}{\partial u^2}e^x - \frac{\partial^2 f}{\partial v \partial u}\sin x \right) e^x + \frac{\partial f}{\partial u}e^x$
		2. 计算第二部分 $\frac{d}{dx} \left( \frac{\partial f}{\partial v}\sin x \right)$ 
			1. 同样使用乘法法则：
			    $\frac{d}{dx} \left( \frac{\partial f}{\partial v}\sin x \right) = \left[ \frac{d}{dx} \left( \frac{\partial f}{\partial v} \right) \right] \cdot \sin x + \frac{\partial f}{\partial v} \cdot \left[ \frac{d}{dx}(\sin x) \right]$
			    *   $\frac{d}{dx}(\sin x) = \cos x$。
			    *   对 $\frac{\partial f}{\partial v}$ 使用链式法则：
			        $\frac{d}{dx} \left( \frac{\partial f}{\partial v} \right) = \frac{\partial}{\partial u}\left(\frac{\partial f}{\partial v}\right)\frac{du}{dx} + \frac{\partial}{\partial v}\left(\frac{\partial f}{\partial v}\right)\frac{dv}{dx} = \frac{\partial^2 f}{\partial u \partial v}e^x + \frac{\partial^2 f}{\partial v^2}(-\sin x)$
			    将这些结果代入，得到第二部分：
			    $\left( \frac{\partial^2 f}{\partial u \partial v}e^x - \frac{\partial^2 f}{\partial v^2}\sin x \right) \sin x + \frac{\partial f}{\partial v}\cos x$
		3. **合并结果**:
		    $\frac{d^2y}{dx^2} = \left[ \left( \frac{\partial^2 f}{\partial u^2}e^x - \frac{\partial^2 f}{\partial v \partial u}\sin x \right) e^x + \frac{\partial f}{\partial u}e^x \right] - \left[ \left( \frac{\partial^2 f}{\partial u \partial v}e^x - \frac{\partial^2 f}{\partial v^2}\sin x \right) \sin x + \frac{\partial f}{\partial v}\cos x \right]$ 
3. 第三步：在 $x=0$ 处求值 
	1. 计算 $u, v$ 的值 
	    当 $x=0$ 时，$u = e^0 = 1$，$v = \cos 0 = 1$。
	    同时，$\sin 0 = 0$，$\cos 0 = 1$，$e^0 = 1$。
	2. **计算 $\frac{dy}{dx}|_{x=0}$**:
	    将 $x=0, u=1, v=1$ 代入一阶导数公式：
	    $\frac{dy}{dx}|_{x=0} = \frac{\partial f(1,1)}{\partial u}e^0 - \frac{\partial f(1,1)}{\partial v}\sin 0 = \frac{\partial f(1,1)}{\partial u} \cdot 1 - \frac{\partial f(1,1)}{\partial v} \cdot 0 = \frac{\partial f(1,1)}{\partial u}$
	3. 计算 $\frac{d^2y}{dx^2}|_{x=0}$:
	    将 $x=0, u=1, v=1$ 代入复杂的二阶导数公式中。所有包含 $\sin 0$ 的项都将变为 0。
	    $\frac{d^2y}{dx^2}|_{x=0} = \left[ \left( \frac{\partial^2 f(1,1)}{\partial u^2}e^0 - \dots \cdot \sin 0 \right) e^0 + \frac{\partial f(1,1)}{\partial u}e^0 \right] - \left[ \left( \dots \right) \sin 0 + \frac{\partial f(1,1)}{\partial v}\cos 0 \right]$
	    简化后：
	    $\frac{d^2y}{dx^2}|_{x=0} = \left[ \frac{\partial^2 f(1,1)}{\partial u^2} \cdot 1 + \frac{\partial f(1,1)}{\partial u} \cdot 1 \right] - \left[ 0 + \frac{\partial f(1,1)}{\partial v} \cdot 1 \right]$
	    最终结果为：
	    $\frac{d^2y}{dx^2}|_{x=0} = \frac{\partial f(1,1)}{\partial u} + \frac{\partial^2 f(1,1)}{\partial u^2} - \frac{\partial f(1,1)}{\partial v}$
- 衍生 
	- 不同的复合形式 
		-  **类型一：$z = f(u(x,y), v(x,y))$**。这时要求解的是偏导数，如 $\frac{\partial z}{\partial x}$ 和 $\frac{\partial^2 z}{\partial x \partial y}$。求解 $\frac{\partial z}{\partial x}$ 的链式法则是：
	        $\frac{\partial z}{\partial x} = \frac{\partial f}{\partial u}\frac{\partial u}{\partial x} + \frac{\partial f}{\partial v}\frac{\partial v}{\partial x}$ 
	    - **类型二：隐函数求导**。例如，由方程 $F(x, y, z) = 0$ 确定了 $z=z(x,y)$，要求 $\frac{\partial z}{\partial x}$。这可以使用公式：
	        $\frac{\partial z}{\partial x} = -\frac{F_x}{F_z}$  #隐函数求导公式 [[偏导数 （隐函数求导公式）]]
	        其中 $F_x$ 是 $F$ 对 $x$ 的偏导数。
	1. 结合全微分 
		1. 要求写出函数的全微分 $dy$ 或 $dz$。对于本题，$dy$ 的表达式为：
		    $dy = \frac{dy}{dx}dx = \left( \frac{\partial f}{\partial u}e^x - \frac{\partial f}{\partial v}\sin x \right)dx$ 
	2. 给出具体函数或偏导数值 
		1. 题目可能不会让您保留抽象的偏导数形式，而是给出具体条件，例如 "$f(u,v) = u^2v$" 或者 "已知 $\frac{\partial f}{\partial u}(1,1) = 2, \frac{\partial f}{\partial v}(1,1) = 3$ 等"，要求计算出最终的数值结果 
	3. #变量替换 与 #偏微分方程 
		1. 在更高级的题目中，链式法则是解决偏微分方程变量替换问题的基础。例如，将直角坐标系下的拉普拉斯方程 $\frac{\partial^2 z}{\partial x^2} + \frac{\partial^2 z}{\partial y^2} = 0$ 变换到极坐标系下 


![[Pasted image 20251004021847.png]]
[[Pasted image 20251004022342.png]]

#极限与级数结合 
[[极限（级数相关）运算法则]] 
[[定积分的定义 (黎曼和)]]
1. 第一步：识别并转化为定积分形式   #黎曼和形式 
	1. #构造黎曼和形式  
	函数 $f(x)$ 在区间 $[a, b]$ 上的定积分被定义为黎曼和的极限：
	$\int_{a}^{b} f(x) dx = \lim_{n \to \infty} \sum_{k=1}^{n} f(\xi_k) \Delta x_k$
	2. 定积分的定义之一是通过黎曼和来逼近的。其标准形式为：
	如果函数 $f(x)$ 在闭区间 $[a, b]$ 上连续，则其定积分为：$\int_a^b f(x) dx = \lim_{n \to \infty} \sum_{k=1}^{n} f(x_k^*) \Delta x$。
		区间 $[a, b]$ 被分成 $n$ 个子区间，每个子区间的宽度为 $\Delta x = \frac{b-a}{n}$，$x_k^*$ 是第 $k$ 个子区间内任意一点 
	3. 最常用的形式是取区间 $[0, 1]$，并取每个子区间的右端点，即 $a=0, b=1$。此时：
		*   $\Delta x = \frac{1-0}{n} = \frac{1}{n}$
		*   $x_k^* = a + k \Delta x = 0 + k \frac{1}{n} = \frac{k}{n}$
		1. $\int_0^1 f(x) dx = \lim_{n \to \infty} \sum_{k=1}^{n} f\left(\frac{k}{n}\right) \frac{1}{n}$ 
	4. 将原式进行变形，以匹配这个结构：
		$\lim_{n \to \infty} \sum_{k=1}^{n} \frac{k}{n^2} \ln \left( 1 + \frac{k}{n} \right) = \lim_{n \to \infty} \sum_{k=1}^{n} \left[ \frac{k}{n} \ln \left( 1 + \frac{k}{n} \right) \right] \cdot \frac{1}{n}$ 
	5. 通过对比，我们可以清晰地看到：
		*   求和的因子 $\frac{1}{n}$ 对应于 $dx$。
		*   表达式中的 $\frac{k}{n}$ 对应于变量 $x$。
		*   整个函数部分 $f\left(\frac{k}{n}\right)$ 就是 $\frac{k}{n} \ln \left( 1 + \frac{k}{n} \right)$。
	6. 函数 $f(x) = x \ln(1+x)$。积分区间由 $\frac{k}{n}$ 的范围决定：当 $k=1$ 时，$\frac{1}{n} \to 0$ (当 $n \to \infty$)；当 $k=n$ 时，$\frac{n}{n} = 1$。所以积分区间是 $[0, 1]$ 
	7. 原式就转化为了一个定积分：$\int_0^1 x \ln(1+x) dx$
2.  第二步：计算定积分（ #分部积分法 ）
	1. $\int u dv = uv - \int v du$ 我们令：
		*   $u = \ln(1+x)$
		*   $dv = x dx$
	2. 计算 $du$ 和 $v$：
		*   $du = \frac{1}{1+x} dx$
		*   $v = \int x dx = \frac{1}{2}x^2$
	3. 代入分部积分公式：
		$\int_0^1 x \ln(1+x) dx = \left[ \frac{1}{2}x^2 \ln(1+x) \right]_0^1 - \int_0^1 \frac{1}{2}x^2 \cdot \frac{1}{1+x} dx$
		$= \left( \frac{1}{2}(1)^2 \ln(1+1) - \frac{1}{2}(0)^2 \ln(1+0) \right) - \frac{1}{2} \int_0^1 \frac{x^2}{1+x} dx$
		$= \frac{1}{2}\ln 2 - \frac{1}{2} \int_0^1 \frac{x^2}{1+x} dx$
3. 第三步：处理剩余的积分（ #有理函数积分 ） 		
	1. 计算积分 $\int_0^1 \frac{x^2}{1+x} dx$。这是一个假分式，我们通过变形将其简化：
		$\frac{x^2}{1+x} = \frac{x^2 - 1 + 1}{1+x} = \frac{(x-1)(x+1) + 1}{1+x} = (x-1) + \frac{1}{1+x}$
	2. 积分就变得简单了：
		$\int_0^1 \frac{x^2}{1+x} dx = \int_0^1 \left( x-1 + \frac{1}{1+x} \right) dx$
		$= \left[ \frac{1}{2}x^2 - x + \ln(1+x) \right]_0^1$
		$= \left( \frac{1}{2}(1)^2 - 1 + \ln(1+1) \right) - \left( \frac{1}{2}(0)^2 - 0 + \ln(1+0) \right)$
		$= \left( \frac{1}{2} - 1 + \ln 2 \right) - (0) = \ln 2 - \frac{1}{2}$
4. 第四步：合并结果，得出最终答案
	1. 将第三步的结果代回到第二步的表达式中：
		原式 $= \frac{1}{2}\ln 2 - \frac{1}{2} \left( \ln 2 - \frac{1}{2} \right)$
		$= \frac{1}{2}\ln 2 - \frac{1}{2}\ln 2 + \frac{1}{4}$
		$= \frac{1}{4}$
		
- 衍生 
	- 黎曼和形式的变化
		-  **积分区间变化**：题目可能构造成积分区间为$[a, b]$的形式，此时$\Delta x = \frac{b-a}{n}$，$x_k = a + k\frac{b-a}{n}$。例如，求$\lim_{n \to \infty} \sum_{k=1}^{n} \frac{1}{n} \sin\left(1+\frac{k}{n}\right)$，对应的积分就是$\int_1^2 \sin(x) dx$（令$t=1+x/n$）或$\int_0^1 \sin(1+x) dx$
	- 与其他知识点结合
	    *   **与级数收敛性结合**：题目可能不是求极限，而是判断一个由积分定义的级数是否收敛。
	    *   **与微分方程结合**：积分的结果可能是一个微分方程的解的一部分。
	    *   **与物理/几何应用结合**：将黎曼和的背景设置为求曲边梯形面积、旋转体体积、曲线弧长或变力做功等实际问题。例如，用积分定义求一个非均匀密度物体的质心。

 

![[Pasted image 20251004021853.png]]
[[Pasted image 20251004022350.png]]

[[隐函数求导]] 
#多元函数求极值  
#驻点   [[二阶导数法判断二元函数内部极值点]] 
1. 第一步：对隐函数方程求一阶和二阶导数 #链式法则
	1. 求一阶导数 $y'$ 
		对方程 $x^3 + y^3 - 3x + 3y - 2 = 0$ 两边关于 $x$ 求导：
	    $\frac{d}{dx}(x^3) + \frac{d}{dx}(y^3) - \frac{d}{dx}(3x) + \frac{d}{dx}(3y) - \frac{d}{dx}(2) = \frac{d}{dx}(0)$
	    *   $\frac{d}{dx}(x^3) = 3x^2$
	    *   $\frac{d}{dx}(y^3) = 3y^2 \cdot \frac{dy}{dx} = 3y^2y'$ (链式法则)
	    *   $\frac{d}{dx}(3x) = 3$
	    *   $\frac{d}{dx}(3y) = 3 \cdot \frac{dy}{dx} = 3y'$ (链式法则)
	    *   常数项导数为 0
	    将这些结果合并，得到一阶导数方程：
	    $3x^2 + 3y^2y' - 3 + 3y' = 0$
	    这就是题解中标注的 **① 式**
2. 求二阶导数 $y''$ #链式法则 #乘法法则  
	对 $3x^2 + 3y^2y' - 3 + 3y' = 0$ 求导：
    $\frac{d}{dx}(3x^2) + \frac{d}{dx}(3y^2y') - \frac{d}{dx}(3) + \frac{d}{dx}(3y') = \frac{d}{dx}(0)$
	 $\frac{d}{dx}(3x^2) = 6x$
    *   $\frac{d}{dx}(3y^2y')$：这里使用乘积法则 $\frac{d}{dx}(uv) = u'v + uv'$。令 $u=3y^2, v=y'$。
        *   $u' = \frac{d}{dx}(3y^2) = 6y \cdot y'$ (链式法则)
        *   $v' = \frac{d}{dx}(y') = y''$
        *   所以 $\frac{d}{dx}(3y^2y') = (6yy') \cdot y' + 3y^2 \cdot y'' = 6y(y')^2 + 3y^2y''$
    *   $\frac{d}{dx}(3y') = 3y''$
	将这些结果合并，得到二阶导数方程：
    $6x + 6y(y')^2 + 3y^2y'' + 3y'' = 0$
3. 第二步：寻找驻点（令 $y'=0$） 
	1. 极值的**必要条件**是 $y'=0$。我们将 $y'=0$ 代入 ① 式：
		$3x^2 + 3y^2(0) - 3 + 3(0) = 0$
		$3x^2 - 3 = 0$
		$x^2 = 1$
		解得 $x = -1$ 或 $x = 1$
4. 第三步：求驻点对应的 $y$ 值 
	1. 将 $x=-1$ 和 $x=1$ 分别代入 
		1.  **当 $x=-1$ 时：**
		    $(-1)^3 + y^3 - 3(-1) + 3y - 2 = 0$
		    $-1 + y^3 + 3 + 3y - 2 = 0$
		    $y^3 + 3y = 0$
		    $y(y^2 + 3) = 0$
		    因为 $y^2+3$ 恒大于 0，所以唯一的实数解是 $y=0$。
		    因此，我们得到一个驻点 $(-1, 0)$。
	2. 当 $x=1$ 时 
		$(1)^3 + y^3 - 3(1) + 3y - 2 = 0$
	    $1 + y^3 - 3 + 3y - 2 = 0$
	    $y^3 + 3y - 4 = 0$
	    通过试根法，我们发现 $y=1$ 是一个解（$1^3+3(1)-4=0$）。我们可以用多项式除法或因式分解：
	    $(y-1)(y^2+y+4) = 0$
	    对于二次项 $y^2+y+4$，其判别式 $\Delta = 1^2 - 4(1)(4) = -15 < 0$，没有实数根。
	    因此，唯一的实数解是 $y=1$。
	    我们得到另一个驻点 $(1, 1)$。
5. 第四步：用二阶导数判别法确定极值类型 
	1. $y''(-1) = 2 > 0$，所以 $y(-1)=0$ 是一个**极小值** 
	2. $y''(1) = -1 < 0$，所以 $y(1)=1$ 是一个**极大值**


- 衍生 
	- 求隐函数在某一点的切线或法线方程
		- 先求出该点的坐标 $(x_0, y_0)$，然后利用一阶导数表达式 ① 计算出切线斜率 $k = y'|_{x=x_0, y=y_0}$。
	    *   切线方程为 $y-y_0 = k(x-x_0)$。
	    *   法线斜率为 $-1/k$，法线方程为 $y-y_0 = -\frac{1}{k}(x-x_0)$
	- 参数方程确定的函数的导数与极值
		- 题目可能给出 $x = f(t), y = g(t)$ 的形式，求 $y$ 关于 $x$ 的极值。
	    *   这需要用到参数方程求导公式：$\frac{dy}{dx} = \frac{dy/dt}{dx/dt}$ 和 $\frac{d^2y}{dx^2} = \frac{d}{dt}(\frac{dy}{dx}) / \frac{dx}{dt}$。
	    *   解题思路类似：令 $\frac{dy}{dx}=0$ 求出参数 $t$ 的值，再用二阶导数判别
	- 与多元函数微积分的结合
		- 将方程 $F(x,y) = x^3 + y^3 - 3x + 3y - 2 = 0$ 视为一个约束条件。
	    *   可能会问：在该曲线上，求 $f(x,y) = x^2+y^2$ 的最值（即曲线上离原点最近或最远的点）。这类问题通常使用 #拉格朗日乘数法 解决[[拉格朗日乘数法]]




![[Pasted image 20251004021858.png]]
[[Pasted image 20251004022405.png]]
[[Pasted image 20251004022430.png]]

#高阶导数对低阶导数的关系   
- (I) 证明 $f(x)=0$ 在 $(0,1)$ 内至少存在一个实根 
1.  证明一个连续函数在一个区间内有根，最常用的方法是 #零点存在定理  [[零点存在性定理(介值定理)]] 
	1. 如果一个连续函数在一个闭区间两端点的函数值异号，那么在这个区间的内部至少存在一个点，使得函数值为零
		1.  **利用条件 $f(1) > 0$：** 我们已经有了一个函数值大于零的点 $x=1$
		2. 利用条件 $\lim_{x \to 0^+} \frac{f(x)}{x} < 0$ 
			1. 根据 #极限的保号性 ，如果一个函数在某点的极限值为一个非零的常数，那么在该点的某个邻域内，函数的符号与极限值的符号相同。
		3. 因为 $\lim_{x \to 0^+} \frac{f(x)}{x} < 0$，所以一定存在一个点 $a$，它充分靠近 $0$ 且在 $(0,1)$ 区间内，使得 $\frac{f(a)}{a} < 0$
		    *   因为 $a \in (0,1)$，所以 $a > 0$。
		    *   由 $\frac{f(a)}{a} < 0$ 和 $a > 0$ 可以推断出 $f(a) < 0$。
	2. 应用零点存在定理 
		1. 现在找到了两个点：$a$ 和 $1$，并且 $0 < a < 1$。
		    *   函数值分别为 $f(a) < 0$ 和 $f(1) > 0$。
		    *   因为 $f(x)$ 在 $[0,1]$ 上可导，所以它在 $[a,1]$ 上必然连续。
		    *   根据零点存在定理，必定存在一个实数 $b \in (a,1)$，使得 $f(b)=0$。
		    *   由于 $(a,1) \subset (0,1)$，所以我们证明了方程 $f(x)=0$ 在区间 $(0,1)$ 内至少存在一个实根
- (II) 证明 $f(x)f''(x) + [f'(x)]^2 = 0$ 在 $(0,1)$ 内至少存在两个不同实根
 1. 证明一个方程有N个根，通常是构造一个辅助函数，然后利用[[罗尔定理]]  
	 1. #罗尔定理 如果一条光滑的曲线（没有断点或尖角）的两个端点在同一高度，那么在这两个端点之间，至少有一个地方，曲线的切线是水平的，使得该点的导数值为零 
		 1. 要证明某个方程有两个根，我们通常需要对它的原函数（辅助函数）应用两次罗尔定理，这意味着我们需要找到这个辅助函数有三个相等的函数值的点。 
	 2. 构造辅助函数
		 1. 观察方程 $f(x)f''(x) + [f'(x)]^2 = 0$ 的左侧。这个结构非常像一个乘积的求导法则的结果 
		 2. 考虑函数 $F(x) = f(x)f'(x)$ 
			 1. 对 $F(x)$ 求导，根据第一次应用：乘积法则：$F'(x) = [f(x)f'(x)]' = f'(x)f'(x) + f(x)f''(x) = [f'(x)]^2 + f(x)f''(x)$
		 3. 原问题就转化为证明方程 $F'(x)=0$ 在 $(0,1)$ 内至少存在两个不同实根 
	 3. 寻找使 $F(x)$ 函数值相等的点 
		 1. 为了证明 $F'(x)=0$ 有两个根，我们需要找到三个点 $x_1, x_2, x_3$，使得 $F(x_1) = F(x_2) = F(x_3)$。最理想的情况是找到三个点使 $F(x)=0$
		 2.  **第一个点 $x=0$：**
	        *   从条件 $\lim_{x \to 0^+} \frac{f(x)}{x}$ 存在且为有限值可知，作为分母的 $x \to 0$，那么作为分子的 $f(x)$ 必须也趋向于 $0$，否则极限为无穷。
	        *   即 $\lim_{x \to 0^+} f(x) = 0$。
	        *   又因为 $f(x)$ 在 $[0,1]$ 上连续，所以 $f(0) = \lim_{x \to 0^+} f(x) = 0$。
	        *   因此，$F(0) = f(0)f'(0) = 0 \cdot f'(0) = 0$。
		3. **第二个点 $x=b$：**
	        *   根据第 (I) 问的结论，存在一个 $b \in (0,1)$ 使得 $f(b)=0$。
	        *   因此，$F(b) = f(b)f'(b) = 0 \cdot f'(b) = 0$。
		4.  **第三个点 $x=c$：**
	        *   我们已经有两个点 $0$ 和 $b$，在这两点上 $f(x)$ 的函数值都为 $0$，即 $f(0)=0$ 和 $f(b)=0$。
	        *   函数 $f(x)$ 在 $[0,b]$ 上连续，在 $(0,b)$ 上可导。
	        *   根据**罗尔定理**，必然存在一个点 $c \in (0,b)$，使得 $f'(c)=0$。
	        *   因此，$F(c) = f(c)f'(c) = f(c) \cdot 0 = 0$。
	4. 应用罗尔定理两次：
		1. 已经找到了三个点 $0, c, b$，满足 $0 < c < b < 1$，并且 $F(0)=F(c)=F(b)=0$
		2. 第一次应用：
			1. 在区间 $[0,c]$ 上，函数 $F(x)$ 满足罗尔定理的条件。因此，存在一点 $\xi \in (0,c)$，使得 $F'(\xi)=0$
		3. 第二次应用
			1.  在区间 $[c,b]$ 上，函数 $F(x)$ 满足罗尔定理的条件。因此，存在一点 $\eta \in (c,b)$，使得 $F'(\eta)=0$
			2. 因为 $\xi \in (0,c)$ 且 $\eta \in (c,b)$，所以 $\xi < c < \eta$，故 $\xi \neq \eta$ 
			3. 同时，$\xi$ 和 $\eta$ 都在 $(0,b)$ 内，而 $(0,b) \subset (0,1)$，所以 $\xi, \eta$ 是方程 $F'(x)=0$ 在 $(0,1)$ 内的两个不同实根。
			    *   即方程 $f(x)f''(x) + [f'(x)]^2 = 0$ 在 $(0,1)$ 内至少存在两个不同实根
* 衍生 
	* 改变辅助函数的构造形式 
		* 若要证明的方程是 $xf'(x) - f(x) = 0$，可以构造辅助函数 $F(x) = \frac{f(x)}{x}$
		* 若要证明的方程是 $f'(x) + f(x) = 0$，可以构造辅助函数 $F(x) = e^x f(x)$，因为 $F'(x) = e^x(f'(x)+f(x))$ 
		* 若要证明的方程是 $f'(x)\sin x + f(x)\cos x = 0$，可以构造辅助函数 $F(x) = f(x)\sin x$
	* 使用不同的中值定理
		* #拉格朗日中值定理   
			* 可能会要求证明形如 $f(b)-f(a) = f'(\xi)(b-a)$ 的相关等式或不等式
		* #柯西中值定理
			* 用于处理两个函数导数之间关系的问题，证明形如 $\frac{f'( \xi)}{g'(\xi)} = \frac{f(b)-f(a)}{g(b)-g(a)}$ 的结论。

![[Pasted image 20251004021904.png]]
[[Pasted image 20251004022437.png]]


[[投影法计算曲面积分]]
[[投影曲线方程]] 
[[曲面的法线方程]]   
[[三重积分的计算方法（切片法）（投影法）]] 
-  (I) 求解投影 #曲线方程   #投影法   #投影曲线方程 
1. 求空间曲线在某个坐标平面（如此处的 $xOy$ 平面）上的投影，本质上是在联立曲线的方程组后，**消去**不属于该平面的坐标变量（此处是 $z$） 
	1. 交线 $C$ 是圆锥面 $z = \sqrt{x^2+y^2}$ 和柱面 $z^2=2x$ 的公共部分，所以它同时满足这两个方程：
		    $\begin{cases} z = \sqrt{x^2+y^2} & (1) \\ z^2 = 2x & (2) \end{cases}$ 
	2. 消去变量 $z$
		1. 由方程 (1) 可得 $z^2 = (\sqrt{x^2+y^2})^2 = x^2+y^2$。
	    -   将这个 $z^2$ 的表达式代入方程 (2)，得到：
	        $x^2+y^2 = 2x$ 
	3. 写出完整的投影方程
		1. 投影是在 $xOy$ 平面上的，这个平面的方程是 $z=0$。因此，投影曲线的完整方程是：
		    $\begin{cases} x^2+y^2=2x \\ z=0 \end{cases}$
		    这个方程表示在 $xOy$ 平面上一个圆心为 $(1,0)$、半径为 1 的圆，因为 $x^2-2x+1+y^2=1 \Rightarrow (x-1)^2+y^2=1$。
-  (II) 求解曲面 S 的质量 M    [[质量的求法]] 
	- **核心思想**：一个物体的质量是其密度在整个物体上的积分。对于一个曲面（薄片），其质量是密度函数在这个曲面上的**第一类曲面积分**。 
1. 建立质量的积分表达式   
	1. 质量 $M$ 等于密度函数 $\mu(x,y,z)$ 在曲面 $S$ 上的第一类曲面积分。
		-   密度函数: $\mu(x,y,z) = 9\sqrt{x^2+y^2+z^2}$
		-   #质量积分公式: $M = \iint_S \mu(x,y,z) \, dS$
		-   代入 #密度函数 :
		    $M = \iint_S 9\sqrt{x^2+y^2+z^2} \, dS$
	2. 将曲面积分转化为二重积分 
		1. 如果曲面 $S$ 的方程是 $z=z(x,y)$，它在 $xOy$ 平面上的投影区域为 $D_{xy}$，那么
		    $\iint_S f(x,y,z) \, dS = \iint_{D_{xy}} f(x,y,z(x,y)) \sqrt{1 + (\frac{\partial z}{\partial x})^2 + (\frac{\partial z}{\partial y})^2} \, dxdy$ 
		2. 确定各部分
			1.  **曲面 S**: $z = \sqrt{x^2+y^2}$ 
			2. **投影区域 $D_{xy}$**: 曲面 $S$ 是被柱面 $z^2=2x$ "割下" 的部分，所以它的边界就是第一问中求出的投影曲线所围成的区域。因此，$D_{xy}$ 是由 $x^2+y^2=2x$ 围成的圆域，即 $D = \{(x,y) | x^2+y^2 \le 2x\}$ 
			3. **计算偏导数**:
		        $\frac{\partial z}{\partial x} = \frac{x}{\sqrt{x^2+y^2}}$
		        $\frac{\partial z}{\partial y} = \frac{y}{\sqrt{x^2+y^2}}$
			4. 计算曲面微元 $dS$
				1. $dS = \sqrt{1 + (\frac{\partial z}{\partial x})^2 + (\frac{\partial z}{\partial y})^2} \, dxdy$
				2. $= \sqrt{1 + \frac{x^2}{x^2+y^2} + \frac{y^2}{x^2+y^2}} \, dxdy = \sqrt{1 + \frac{x^2+y^2}{x^2+y^2}} \, dxdy = \sqrt{2} \, dxdy$
			5. **替换被积函数中的 $z$**: 将 $z=\sqrt{x^2+y^2}$ 代入密度函数中 
				1. $\mu(x,y,z(x,y)) = 9\sqrt{x^2+y^2+(\sqrt{x^2+y^2})^2} = 9\sqrt{2(x^2+y^2)} = 9\sqrt{2}\sqrt{x^2+y^2}$ 
			6.   **组合成二重积分**:
			    $M = \iint_D (9\sqrt{2}\sqrt{x^2+y^2}) (\sqrt{2} \, dxdy) = \iint_D 18\sqrt{x^2+y^2} \, dxdy$
	3. 使用极坐标计算二重积分  [[极坐标系]]
		1. **坐标变换**:
		    $x = r\cos\theta, \quad y = r\sin\theta, \quad dxdy = rdrd\theta$
		-   **变换被积函数**:
		    $18\sqrt{x^2+y^2} = 18r$
		-   **变换积分区域 $D$**:
		    $x^2+y^2 \le 2x$ 变为 $r^2 \le 2r\cos\theta$。因为 $r \ge 0$，两边可以同除以 $r$，得到 $r \le 2\cos\theta$。
		    同时，为了保证 $r \ge 0$，必须有 $\cos\theta \ge 0$，这决定了 $\theta$ 的范围是 $-\frac{\pi}{2} \le \theta \le \frac{\pi}{2}$。
		    所以，积分区域的极坐标表示为：
		    $D = \{(r,\theta) | -\frac{\pi}{2} \le \theta \le \frac{\pi}{2}, 0 \le r \le 2\cos\theta \}$
		1. **计算积分**:
		    $M = \int_{-\pi/2}^{\pi/2} d\theta \int_0^{2\cos\theta} (18r) \cdot r \, dr = 18 \int_{-\pi/2}^{\pi/2} \left[ \int_0^{2\cos\theta} r^2 \, dr \right] d\theta$
		    先计算内层关于 $r$ 的积分：
		    $\int_0^{2\cos\theta} r^2 \, dr = \left[ \frac{r^3}{3} \right]_0^{2\cos\theta} = \frac{(2\cos\theta)^3}{3} = \frac{8}{3}\cos^3\theta$
		    再计算外层关于 $\theta$ 的积分：
		    $M = 18 \int_{-\pi/2}^{\pi/2} \frac{8}{3}\cos^3\theta \, d\theta = 48 \int_{-\pi/2}^{\pi/2} \cos^3\theta \, d\theta$
		    对于 $\int \cos^3\theta \, d\theta$，这是一个三角函数积分。由于 $\cos^3\theta$ 是偶函数，且积分区间 $[-\frac{\pi}{2}, \frac{\pi}{2}]$ 对称，我们可以用两倍的半区间积分来计算：
		    $\int_{-\pi/2}^{\pi/2} \cos^3\theta \, d\theta = 2 \int_0^{\pi/2} \cos^3\theta \, d\theta$    [[降幂公式]] 
		    也可以使用 #瓦利斯积分公式 $\int_0^{\pi/2} \cos^n x \, dx = \frac{(n-1)!!}{n!!}$  
		    对于 $n=3$，结果是 $\frac{2}{3}$。
		    所以 $\int_{-\pi/2}^{\pi/2} \cos^3\theta \, d\theta = 2 \times \frac{2}{3} = \frac{4}{3}$。
		    最后得到质量：
		    $M = 48 \times \frac{4}{3} = 16 \times 4 = 64$

- 衍生 
	- #第二类曲面积分 
		- 这是曲面积分的另一个重要类型，形式为 $\iint_S P dy dz + Q dz dx + R dx dy$ 或 $\iint_S \vec{F} \cdot d\vec{S}$。它通常表示向量场（如流速场、电场）穿过某个曲面的通量
	- #第一类曲线积分  
		- 与本题类似，但积分对象是一条曲线而非曲面，如计算一段密度不均匀的铁丝的质量 $\int_L \mu(x,y,z)ds$
	- #参数方程表示的曲面积分 如果曲面由参数方程 $\vec{r}(u,v)$ 给出，那么曲面微元 $dS = ||\frac{\partial \vec{r}}{\partial u} \times \frac{\partial \vec{r}}{\partial v}|| du dv$。这是更通用的计算方法 




![[Pasted image 20251004021908.png]]
[[Pasted image 20251004022446.png]]
[[Pasted image 20251004022452.png]]


[[特征值和特征向量的定义]]
[[秩与特征值0的关系]]

[[Ax=b线性方程组的解的结构与矩阵的基本子空间]] 
[[齐次线性方程组的解Ax=0]]
- (I) 证明 $r(A)=2$ 
	1. 第一步：证明 $r(A) \le 2$ 
	2. 矩阵的秩等于其列向量组的秩    [[秩的性质]]
		1. 题目给出了条件 $\alpha_3 = \alpha_1 + 2\alpha_2$。我们可以将其变形为：
		    $1 \cdot \alpha_1 + 2 \cdot \alpha_2 - 1 \cdot \alpha_3 = 0$
		    这表明向量组 $\alpha_1, \alpha_2, \alpha_3$ 是**线性相关**的
		2. 一个矩阵的秩等于其行秩，也等于其列秩，即其线性无关的行/列向量的最大数目 
			1. 得出结论 $r(A) < 3$，即 $r(A) \le 2$ 
	3. 第二步：证明 $r(A) \ge 2$
		1.    [[特征值和特征向量的定义]]
			1. 如果一个非零向量 $\xi$ 和一个数 $\lambda$ 满足方程 $A\xi = \lambda\xi$，那么 $\lambda$ 称为矩阵 $A$ 的一个 #特征值 ，$\xi$ 称为 $A$ 对应于特征值 $\lambda$ 的一个 #特征向量
			2. 如果 $Ax=0$ 有非零解，则 $\lambda=0$ 是矩阵 $A$ 的一个特征值 
		2.  [[相似对角化]]   [[矩阵可对角化的条件]]
			1. 一个$n \times n$矩阵如果有$n$个互不相同的 #特征值 ，则它一定可以 #相似对角化
		3. 能相似对角化的矩阵，其秩等于 #非零特征值 的个数（计入重数） 
	4. 推导过程
		1. 从第一步的式子 $\alpha_1 + 2\alpha_2 - \alpha_3 = 0$ 出发，我们可以利用矩阵乘法的定义将其写成矩阵方程的形式：
	        $A \begin{pmatrix} 1 \\ 2 \\ -1 \end{pmatrix} = (\alpha_1, \alpha_2, \alpha_3) \begin{pmatrix} 1 \\ 2 \\ -1 \end{pmatrix} = 1\alpha_1 + 2\alpha_2 - 1\alpha_3 = 0$ 
	    2. 令 $x_0 = \begin{pmatrix} 1 \\ 2 \\ -1 \end{pmatrix}$，我们得到 $Ax_0 = 0$。由于 $x_0$ 是一个非零向量，这说明齐次线性方程组 $Ax=0$ 有非零解 
		3. 特征值的定义 $Ax=\lambda x$，当 $\lambda=0$ 时，方程变为 $Ax=0$。存在非零解 $x_0$ 意味着 $\lambda_1 = 0$ 就是矩阵 $A$ 的一个特征值
		4. 指出 $A$ 有3个**不同**的特征值。既然其中一个是0，那么另外两个特征值 $\lambda_2$ 和 $\lambda_3$ 必定不为0，且互不相等 
		5. 因为 $A$ 有3个不同的特征值，所以 $A$ 可以相似对角化。对于可对角化的矩阵，其秩等于 #非零特征值 的个数 
		6. 已经确定 $A$ 有两个非零特征值（$\lambda_2, \lambda_3$），所以 $r(A)=2$。这自然也满足 $r(A) \ge 2$ 
	5. 综合以上两步，我们有 $r(A) \le 2$ 和 $r(A) \ge 2$，因此可以断定 $r(A)=2$

- (II) 求解 $Ax=\beta$ 的通解   [[二阶常系数非齐次线性微分方程求解]] [[Ax=b线性方程组的解的结构与矩阵的基本子空间]]  
1.  $Ax=b$ 的通解关系 
	1. 假设我们找到了非齐次方程组 $Ax=b$ 的一个 特解 记为 $x_p$。也就是说 $Ax_p = b$ 
	2. 同时，我们知道齐次方程组 $Ax=0$ 的通解是 $x_h$，其中 $x_h$ 是零空间中的任意一个向量。 
		1. 来看 $x_p + x_h$ 这个向量 
			1. $A(x_p + x_h) = Ax_p + Ax_h = b + 0 = b$ 
			2. $x_p + x_h$ 也是方程 $Ax=b$ 的一个解 
	3. 如果非齐次方程组 $Ax=b$有解，那么它的 #通解结构 是 通解 = 一个特解 + 对应齐次方程组的通解 
		1. $x_{通解} = x_p + x_h$ 
		2. 其中 $x_h$ 来自于矩阵 $A$ 的零空间 $Nul(A)$

2. 第一步：求对应的齐次方程组 $Ax=0$ 的通解 $x_h$   [[秩与解空间维数的关系]]
	1. #基础解系 中向量的个数等于自由变量的个数，即 $n - r(A)$，其中 $n$ 是未知数的个数（矩阵 $A$ 的列数），$r(A)$ 是矩阵 $A$ 的秩。 
	2. 在第(I)问中，我们已经找到了 $Ax=0$ 的一个非零解：$\eta = \begin{pmatrix} 1 \\ 2 \\ -1 \end{pmatrix}$ 
	3. 证明了 $r(A)=2$。对于这个3阶矩阵，$Ax=0$ 的解空间的维数是 $n - r(A) = 3 - 2 = 1$ 
	4. 意味着 $Ax=0$ 的基础解系只包含一个线性无关的解向量。我们找到的非零解 $\eta$ 正好可以作为这个基础解系的基 
	5. 因此，齐次方程组 $Ax=0$ 的通解为 $x_h = k\eta = k\begin{pmatrix} 1 \\ 2 \\ -1 \end{pmatrix}$，其中 $k$ 是任意常数 
3. 第二步：求非齐次方程组 $Ax=\beta$ 的一个特解 $x_p$ 
	1. 特解是满足 $Ax_p=\beta$ 的任意一个解向量。当 $\beta$ 可以被简单地表示为 $A$ 的列向量的线性组合时，可以通过观察直接写出特解 
		1. 题目给出了 $\beta = \alpha_1 + \alpha_2 + \alpha_3$。
		2. 再次利用矩阵乘法的定义：$Ax = (\alpha_1, \alpha_2, \alpha_3) \begin{pmatrix} x_1 \\ x_2 \\ x_3 \end{pmatrix} = x_1\alpha_1 + x_2\alpha_2 + x_3\alpha_3$ 
		3. 将这个定义与 $\beta$ 的表达式进行对比，我们不难发现，如果取 $x_1=1, x_2=1, x_3=1$，那么：
	        $A \begin{pmatrix} 1 \\ 1 \\ 1 \end{pmatrix} = 1\cdot\alpha_1 + 1\cdot\alpha_2 + 1\cdot\alpha_3 = \beta$ 
	    4. 说明 $x_p = \begin{pmatrix} 1 \\ 1 \\ 1 \end{pmatrix}$ 就是方程组 $Ax=\beta$ 的一个特解
4. 第三步：组合得到通解 
	1.   **推导过程：**
	    将特解 $x_p$ 和齐次通解 $x_h$ 相加，得到 $Ax=\beta$ 的通解：
	    $x = x_p + x_h = \begin{pmatrix} 1 \\ 1 \\ 1 \end{pmatrix} + k\begin{pmatrix} 1 \\ 2 \\ -1 \end{pmatrix}$，其中 $k$ 为任意常数。


- 衍生 
	- #迹与特征值的关系  题目可能还会给出矩阵的迹 $tr(A)$（主对角线元素之和）。因为迹等于所有特征值之和 ($tr(A) = \lambda_1+\lambda_2+\lambda_3$)，结合 $\lambda_1=0$，就可以得到另外两个特征值之间的关系 $\lambda_2+\lambda_3 = tr(A)$。
	- #行列式与特征值的关系  
		- 题目可以不直接给列向量关系，而是给出 $det(A)=0$。因为行列式等于所有特征值的乘积 ($det(A) = \lambda_1\lambda_2\lambda_3$)，所以 $det(A)=0$ 同样可以推断出至少有一个特征值为0。
	- 求解基础解系 
		- 如果题目没有直接给出列向量关系，而是给出了矩阵 $A$ 的具体形式，那么就需要通过高斯消元法来求 $r(A)$ 和 $Ax=0$ 的基础解系 
	- 判断解的存在性 
		- 对于非齐次方程组 $Ax=\beta$，它有解的充要条件是 $r(A) = r(A|\beta)$，其中 $(A|\beta)$ 是增广矩阵。在本题中，因为 $\beta$ 明确是 $A$ 的列向量的线性组合，所以 $\beta$ 属于 $A$ 的列空间，方程组必然有解。如果给一个任意的 $\beta$，则需要先判断解是否存在 
 








![[Pasted image 20251004021915.png]]
[[Pasted image 20251004022502.png]]

[[二次型]]  [[正交变换]]  
#标准型  

1. 第一步：写出二次型对应的矩阵A 
	1. 任何一个二次型 $f(\mathbf{x}) = \mathbf{x}^T A \mathbf{x}$ 都可以由一个对称矩阵 $A$ 表示。矩阵 $A$ 的构建规则如下：
		*   主对角线元素 $a_{ii}$ 是 $x_i^2$ 项的系数。
		*   非对角线元素 $a_{ij}$ (其中 $i \neq j$) 是 $x_i x_j$ 项系数的一半。
	2. 所以，二次型对应的对称矩阵 $A$ 为：
		$A=\begin{bmatrix} 2 & 1 & -4 \\ 1 & -1 & 1 \\ -4 & 1 & a \end{bmatrix}$ 
2. 第二步：利用标准型信息求参数a  [[二次型的标准型]]
	1. 通过正交变换 $\mathbf{x} = Q\mathbf{y}$，二次型 $f(\mathbf{x}) = \mathbf{x}^T B \mathbf{x}$ 可以化为 #标准形  $f(\mathbf{y}) = \mathbf{y}^T (Q^T B Q) \mathbf{y} = \mathbf{y}^T \Lambda \mathbf{y} = \lambda_1 y_1^2 + \lambda_2 y_2^2 + \dots + \lambda_n y_n^2$。标准形中的系数就是矩阵  的特征值。 
			题目中给出的标准形是 $\lambda_1y_1^2+\lambda_2y_2^2$，这是一个三元二次型，但标准形中只有两项。这意味着它的三个特征值中，有一个为0。
	2. [[行列式与特征值的关系]]
		矩阵的行列式等于其所有特征值的乘积即 $|A|=\lambda_1\lambda_2\lambda_3$。
	3. 既然有一个特征值为0（不妨设 $\lambda_3=0$），那么必然有 $|A|=0$。我们来计算矩阵 $A$ 的行列式：
		$|A| = 2\begin{vmatrix} -1 & 1 \\ 1 & a \end{vmatrix} - 1\begin{vmatrix} 1 & 1 \\ -4 & a \end{vmatrix} + (-4)\begin{vmatrix} 1 & -1 \\ -4 & 1 \end{vmatrix}$
		$|A| = 2(-a-1) - 1(a - (-4)) - 4(1-4)$
		$|A| = -2a-2 - (a+4) - 4(-3)$
		$|A| = -2a-2-a-4+12$
		$|A| = -3a+6$
		根据 $|A|=0$，我们得到：
		$-3a+6=0 \implies a=2$
3. 第三步：求解矩阵A的特征值 
	1. 已经确定 $a=2$，矩阵 $A$ 为：
		$A=\begin{bmatrix} 2 & 1 & -4 \\ 1 & -1 & 1 \\ -4 & 1 & 2 \end{bmatrix}$
	2. 求解 $A$ 的特征值，即解特征方程 $|\lambda E-A|=0$：
		$|\lambda E-A| = \begin{vmatrix} \lambda-2 & -1 & 4 \\ -1 & \lambda+1 & -1 \\ 4 & -1 & \lambda-2 \end{vmatrix}=0$ 
	3. 展开计算这个行列式 
		$=(\lambda-2)[(\lambda+1)(\lambda-2)-1] - (-1)[-(\lambda-2)+4] + 4[1-4(\lambda+1)]$
		$=(\lambda-2)(\lambda^2-\lambda-3) + (-\lambda+6) + 4(-4\lambda-3)$
		$= \lambda^3-\lambda^2-3\lambda-2\lambda^2+2\lambda+6-\lambda+6-16\lambda-12$
		$= \lambda^3-3\lambda^2-18\lambda = \lambda(\lambda^2-3\lambda-18) = \lambda(\lambda-6)(\lambda+3)=0$
	4. 解得三个特征值为：
		$\lambda_1=-3, \lambda_2=6, \lambda_3=0$ 
4. 第四步：求解每个特征值对应的特征向量  [[如何求特征值]] 
	1. 对于每个特征值 $\lambda_i$，其对应的特征向量 $x$ 是齐次线性方程组 $(\lambda_i E - A)x = 0$ 的非零解。
		1.   **当 $\lambda_1=-3$ 时**，解方程 $(-3E-A)x=0$：
		    $\begin{bmatrix} -5 & -1 & 4 \\ -1 & -2 & -1 \\ 4 & -1 & -5 \end{bmatrix} \begin{bmatrix} x_1 \\ x_2 \\ x_3 \end{bmatrix} = \begin{bmatrix} 0 \\ 0 \\ 0 \end{bmatrix}$
		    通过行变换化简系数矩阵，可得一个基础解系的向量为 $\alpha_1=(1, -1, 1)^T$。
		
		2.  **当 $\lambda_2=6$ 时**，解方程 $(6E-A)x=0$：
		    $\begin{bmatrix} 4 & -1 & 4 \\ -1 & 7 & -1 \\ 4 & -1 & 4 \end{bmatrix} \begin{bmatrix} x_1 \\ x_2 \\ x_3 \end{bmatrix} = \begin{bmatrix} 0 \\ 0 \\ 0 \end{bmatrix}$
		    通过行变换化简，可得一个基础解系的向量为 $\alpha_2=(-1, 0, 1)^T$。
		
		3.  **当 $\lambda_3=0$ 时**，解方程 $(0E-A)x=0$，即 $-Ax=0$：
		    $\begin{bmatrix} -2 & -1 & 4 \\ -1 & 1 & -1 \\ 4 & -1 & -2 \end{bmatrix} \begin{bmatrix} x_1 \\ x_2 \\ x_3 \end{bmatrix} = \begin{bmatrix} 0 \\ 0 \\ 0 \end{bmatrix}$
		    通过行变换化简，可得一个基础解系的向量为 $\alpha_3=(1, 2, 1)^T$。
1. 第五步： #单位化特征向量 并构造 #正交矩阵Q   [[单位化]]  
	1. 实对称矩阵的不同特征值对应的特征向量是**正交**的    
	2. 正交矩阵 $Q$ 的列向量是由一组标准正交基构成的。我们需要将上一步求出的特征向量 $\alpha_1, \alpha_2, \alpha_3$ 进行**单位化**。 
		1. #单位化公式 为：$\beta = \frac{\alpha}{||\alpha||}$，其中 $||\alpha|| = \sqrt{x_1^2+x_2^2+...}$ 是向量的模  
		 $\alpha_1=(1, -1, 1)^T$, $||\alpha_1||=\sqrt{1^2+(-1)^2+1^2}=\sqrt{3}$
		    $\beta_1 = \frac{1}{\sqrt{3}}(1, -1, 1)^T$
		
		*   $\alpha_2=(-1, 0, 1)^T$, $||\alpha_2||=\sqrt{(-1)^2+0^2+1^2}=\sqrt{2}$
		    $\beta_2 = \frac{1}{\sqrt{2}}(-1, 0, 1)^T$
		
		*   $\alpha_3=(1, 2, 1)^T$, $||\alpha_3||=\sqrt{1^2+2^2+1^2}=\sqrt{6}$
		    $\beta_3 = \frac{1}{\sqrt{6}}(1, 2, 1)^T$
		
		将这些单位化的特征向量作为列向量，构成正交矩阵 $Q$：
		$Q = [\beta_1, \beta_2, \beta_3] = \begin{bmatrix} \frac{1}{\sqrt{3}} & -\frac{1}{\sqrt{2}} & \frac{1}{\sqrt{6}} \\ -\frac{1}{\sqrt{3}} & 0 & \frac{2}{\sqrt{6}} \\ \frac{1}{\sqrt{3}} & \frac{1}{\sqrt{2}} & \frac{1}{\sqrt{6}} \end{bmatrix}$


- 衍生 
	- #二次型的正定性判断 
		- #正定性问题   考点：根据特征值的正负来判断二次型是正定、负定、半正定、半负定还是不定。
		    *   此题中特征值为-3, 6, 0，有正有负，所以该二次型是**不定的**。如果特征值全为正，则为正定二次型。
	* 求二次型在约束条件下的最值 
		*  考点：在约束条件 $x^Tx=1$ 下，二次型 $f(x)=x^TAx$ 的最大值是矩阵 $A$ 的最大特征值，最小值是最小特征值。
	    *   此题中，在 $x_1^2+x_2^2+x_3^2=1$ 的条件下，$f(x)$ 的最大值为 $\lambda_{max}=6$，最小值为 $\lambda_{min}=-3$。
	* #合同变换与合同标准型 #合同变换   
		-  考点：不要求是正交变换，只用可逆线性变换 $x=Cy$ 化二次型为标准形。这种方法（如配方法）得到的新系数不一定是特征值，但正、负、零系数的个数（正负惯性指数）是唯一的。
	    *   此题中，正惯性指数为1（一个正特征值6），负惯性指数为1（一个负特征值-3），秩为2（两个非零特征值）。
	* 几何意义
		*  考点：二次曲面 $f(x_1,x_2,x_3)=k$ 的形状。正交变换 $x=Qy$ 相当于对坐标系进行了一次旋转，将坐标轴旋转到二次曲面的主轴方向上，从而简化方程，判断其几何形态（如椭球面、双曲面等）
	* #矩阵的对角化  
		* 考点：求可逆矩阵 $P$ 使得 $P^{-1}AP=\Lambda$（对角矩阵）。对于实对称矩阵 $A$，总存在正交矩阵 $Q$ 使得 $Q^{-1}AQ = Q^TAQ = \Lambda$。
		    *   本题求出的 $Q$ 就可以用来对角化 $A$，且对角矩阵 $\Lambda$ 的对角线元素就是特征值-3, 6, 0（顺序与 $Q$ 的列向量对应）。
		    $\Lambda = \begin{bmatrix} -3 & 0 & 0 \\ 0 & 6 & 0 \\ 0 & 0 & 0 \end{bmatrix}$



![[Pasted image 20251004021919.png]]
[[Pasted image 20251004022512.png]]
[[Pasted image 20251004022530.png]]


[[概率密度函数的性质]] 
[[概率密度函数的定义]]  
[[期望与方差的性质和定义]] 
[[求解期望的两种常用方法]] 
-  (I) 求 $P\{Y \le E(Y)\}$ 
	1. 第一步：计算Y的期望 $E(Y)$ 
		1. #连续型随机变量的数学期望 [[连续型随机变量的数学期望]]
			1. 对于一个连续型随机变量 $X$，其数学期望的计算公式为：
			    $E(X) = \int_{-\infty}^{+\infty} x f(x) dx$  
				    $E(Y) = \int_{-\infty}^{+\infty} y f(y) dy$
			*   **在本题中：** Y的概率密度函数 $f(y)$ 是一个分段函数，只在 $(0, 1)$ 区间内非零。因此，积分的上下限从 $(-\infty, +\infty)$ 缩减到  $(0, 1)$。
			* $E(Y) = \int_{0}^{1} y \cdot (2y) dy = \int_{0}^{1} 2y^2 dy$
			* **计算积分：**
			    $\int_{0}^{1} 2y^2 dy = 2 \left[ \frac{y^3}{3} \right]_0^1 = 2 \left( \frac{1^3}{3} - \frac{0^3}{3} \right) = \frac{2}{3}$
			    所以，我们得到了 $E(Y) = \frac{2}{3}$。 
	2. 第二步：计算概率 $P\{Y \le \frac{2}{3}\}$ 
		1. #概率密度函数的定义  对于任意区间 $[a, b]$，变量落入该区间的概率为 $P(a \le X \le b) = \int_a^b f(x)dx$   
			1. $P\{a \le Y \le b\} = \int_{a}^{b} f(y) dy$ 
		2.  **在本题中：** 我们要求 $P\{Y \le \frac{2}{3}\}$。因为Y的取值范围是 $(0, 1)$，所以这等价于求 $P\{0 < Y \le \frac{2}{3}\}$。
			$P\{Y \le \frac{2}{3}\} = \int_{-\infty}^{2/3} f(y) dy = \int_{0}^{2/3} 2y dy$
			*   **计算积分：** 
			    $\int_{0}^{2/3} 2y dy = \left[ y^2 \right]_0^{2/3} = (\frac{2}{3})^2 - 0^2 = \frac{4}{9}$
			    因此，第一问的答案是 $P\{Y \le E(Y)\} = \frac{4}{9}$
-  (II) 求 $Z=X+Y$ 的概率密度 
1. 第一步：定义Z的分布函数 $F_Z(z)$ [[（累计）分布函数的定义]]
	1. #分布函数的定义  Z的分布函数定义为 $F_Z(z) = P\{Z \le z\}$。我们的目标是先求出 $F_Z(z)$，然后通过求导得到其概率密度函数 $f_Z(z)$，即 $f_Z(z) = F_Z'(z)$。
		    $F_Z(z) = P\{X+Y \le z\}$   
2. 第二步：使用全概率公式进行分解 
	1. #全概率公式  [[全概率公式]]
		1. 由于X是离散的，它只取0和2两个值。我们可以根据X的取值，使用全概率公式将事件 $\{X+Y \le z\}$ 分解为两个互斥事件的并集：
		    $P\{X+Y \le z\} = P\{X+Y \le z, X=0\} + P\{X+Y \le z, X=2\}$
		*   利用 #条件概率公式 $P(AB)=P(A|B)P(B)$，上式可以写为：
			$P\{X+Y \le z\} = P\{X+Y \le z | X=0\}P\{X=0\} + P\{X+Y \le z | X=2\}P\{X=2\}$
3. 第三步：利用独立性简化表达式 
	1. [[随机变量的独立性]]  
	2. $X$ 和 $Y$ 相互独立。这意味着两个事件同时发生的概率是它们各自概率的乘积。例如：
		$P(X=x, Y=y) = P(X=x)P(Y=y)$  
	    *   当 $X=0$ 时，$P\{X+Y \le z | X=0\}$ 变为 $P\{0+Y \le z\} = P\{Y \le z\}$。
	    *   当 $X=2$ 时，$P\{X+Y \le z | X=2\}$ 变为 $P\{2+Y \le z\} = P\{Y \le z-2\}$。 
	3. 将 $P\{X=0\}=\frac{1}{2}$ 和 $P\{X=2\}=\frac{1}{2}$ 代入，我们得到 $F_Z(z)$ 的关键表达式：
	    $F_Z(z) = \frac{1}{2}P\{Y \le z\} + \frac{1}{2}P\{Y \le z-2\}$ 
	4. 令 $F_Y(y)$ 为Y的分布函数，则上式可以写为：
	    $F_Z(z) = \frac{1}{2}F_Y(z) + \frac{1}{2}F_Y(z-2)$  
4. 第四步：求出Y的分布函数 $F_Y(y)$ 
	1. 进行下一步计算，我们先求出 $F_Y(y) = P\{Y \le y\} = \int_{-\infty}^{y} f(t) dt$。
    *   当 $y \le 0$ 时，$F_Y(y) = \int_{-\infty}^{y} 0 dt = 0$。
	    *   当 $0 < y \le 1$ 时，$F_Y(y) = \int_{0}^{y} 2t dt = [t^2]_0^y = y^2$。
	    *   当 $y > 1$ 时，$F_Y(y) = \int_{0}^{1} 2t dt = 1$。
	*   所以，Y的分布函数为：
	    $F_Y(y) = \begin{cases} 0, & y \le 0 \\ y^2, & 0 < y \le 1 \\ 1, & y > 1 \end{cases}$
5. 第五步：分段讨论，求出 $F_Z(z)$  #分情况 
	1.   **当 $z < 0$ 时：**
	    $z < 0$ 且 $z-2 < -2$。此时 $F_Y(z)=0$ 且 $F_Y(z-2)=0$
	    $F_Z(z) = \frac{1}{2}(0) + \frac{1}{2}(0) = 0$。
	2. **当 $0 \le z < 1$ 时：**
	    $0 \le z < 1$ 且 $-2 \le z-2 < -1$。此时 $F_Y(z)=z^2$ 且 $F_Y(z-2)=0$。
	    $F_Z(z) = \frac{1}{2}(z^2) + \frac{1}{2}(0) = \frac{z^2}{2}$ 
	3. **当 $1 \le z < 2$ 时：**
	    $1 \le z < 2$ 且 $-1 \le z-2 < 0$。此时 $z>1$，所以 $F_Y(z)=1$。而 $z-2 \le 0$，所以 $F_Y(z-2)=0$。
	    $F_Z(z) = \frac{1}{2}(1) + \frac{1}{2}(0) = \frac{1}{2}$。
	4.   **当 $2 \le z < 3$ 时：**
	    $2 \le z < 3$ 且 $0 \le z-2 < 1$。此时 $z>1$，所以 $F_Y(z)=1$。而 $0 < z-2 \le 1$，所以 $F_Y(z-2)=(z-2)^2$。
	    $F_Z(z) = \frac{1}{2}(1) + \frac{1}{2}(z-2)^2 = \frac{1}{2} + \frac{1}{2}(z-2)^2$。
	
	*   **当 $z \ge 3$ 时：**
	    $z \ge 3$ 且 $z-2 \ge 1$。此时 $z>1$ 且 $z-2>1$，所以 $F_Y(z)=1$ 且 $F_Y(z-2)=1$。
	    $F_Z(z) = \frac{1}{2}(1) + \frac{1}{2}(1) = 1$。
1. 第六步：对 $F_Z(z)$ 求导得到概率密度函数 $f_Z(z)$  
	1. 概率密度函数与分布函数的关系 $f(x)=F'(x)$。
	*   我们对上面求出的分段分布函数 $F_Z(z)$ 在各个区间内求导：
	    *   当 $0 < z < 1$ 时，$f_Z(z) = \frac{d}{dz}(\frac{z^2}{2}) = z$。
	    *   当 $1 < z < 2$ 时，$f_Z(z) = \frac{d}{dz}(\frac{1}{2}) = 0$。
	    *   当 $2 < z < 3$ 时，$f_Z(z) = \frac{d}{dz}(\frac{1}{2} + \frac{1}{2}(z-2)^2) = \frac{1}{2} \cdot 2(z-2) = z-2$。
	    *   在其他区间 ($z<0$ 或 $z>3$)，$F_Z(z)$ 是常数，导数为0。
	  **最终结果：** 将以上结果合并，得到Z的概率密度函数：
    $f_Z(z) = \begin{cases} z, & 0 < z < 1 \\ z-2, & 2 < z < 3 \\ 0, & \text{其他} \end{cases}$
 - 衍生 
	 - [[卷积公式]]  
		 - 如果考察两个独立随机变量之和$W = X_1 + X_2$的概率密度，则其密度函数$f_W(w)$由 #卷积公式 给出：
		    $f_W(w) = \int_{-\infty}^{+\infty} f_1(x)f_2(w-x)dx$  
	

![[Pasted image 20251004021924.png]]
[[Pasted image 20251004022540.png]]


[[正态分布的概率密度函数]]  


- #一阶矩（期望）   
	- [[矩估计法]]  

-  (I) 求 $Z_i$ 的概率密度函数 
	1. 求 $Z_i$ 的分布函数 $F_{Z_i}(z)$   [[（累计）分布函数的定义]]
		1. 对于任意一个随机变量 $Z$，其 #分布函数 定义为 $F_Z(z) = P(Z \le z)$。  
		2. 根据题意，$Z_i = |X_i - \mu|$，其中 $X_i \sim N(\mu, \sigma^2)$ 
	2. 当 $z < 0$ 时，因为绝对值 $Z_i$ 不可能为负，所以事件 $Z_i \le z$ 是不可能事件，其概率为0。
	    $F_{Z_i}(z) = P(Z_i \le z) = 0$
	3.  当 $z \ge 0$ 时：
	    $F_{Z_i}(z) = P(Z_i \le z) = P(|X_i - \mu| \le z)$
	    这是一个绝对值不等式，可以展开为：
	    $P(-z \le X_i - \mu \le z)$
	    为了利用标准正态分布，我们对 $X_i - \mu$ 进行标准化。已知 $X_i \sim N(\mu, \sigma^2)$，则 $X_i - \mu \sim N(0, \sigma^2)$。将其除以标准差 $\sigma$，得到标准正态分布变量 $\frac{X_i - \mu}{\sigma} \sim N(0, 1)$。
	    不等式两边同除以 $\sigma$（$\sigma > 0$）：
	    $P\left(-\frac{z}{\sigma} \le \frac{X_i - \mu}{\sigma} \le \frac{z}{\sigma}\right)$
	    这等价于标准正态分布变量落在 $[-\frac{z}{\sigma}, \frac{z}{\sigma}]$ 区间内的概率。用标准正态分布的分布函数 $\Phi(\cdot)$ 表示为：
	    $\Phi\left(\frac{z}{\sigma}\right) - \Phi\left(-\frac{z}{\sigma}\right)$
	    利用标准正态分布的对称性 $\Phi(-x) = 1 - \Phi(x)$，上式可化为：
	    $\Phi\left(\frac{z}{\sigma}\right) - \left(1 - \Phi\left(\frac{z}{\sigma}\right)\right) = 2\Phi\left(\frac{z}{\sigma}\right) - 1$
	
	综上，$Z_i$ 的分布函数为：
	$F_{Z_i}(z) = \begin{cases} 2\Phi\left(\frac{z}{\sigma}\right) - 1, & z \ge 0 \\ 0, & z < 0 \end{cases}$
	1.  求 $Z_i$ 的概率密度函数 $f_{Z_i}(z)$ 
		1. #PDF和CDF的关系 
			1.      $F'(x) = \frac{d}{dx}F(x) = f(x)$ 
				1. $f_{Z_i}(z) = \frac{d}{dz}F_{Z_i}(z)$ 
			2. 我们对分段函数求导：
			*   当 $z < 0$ 时，$f_{Z_i}(z) = \frac{d}{dz}(0) = 0$。
			
			*   当 $z \ge 0$ 时，我们需要对 $2\Phi(\frac{z}{\sigma}) - 1$ 求导。
			    回顾一下，标准正态分布的概率密度函数为 $\phi(x) = \frac{1}{\sqrt{2\pi}}e^{-\frac{x^2}{2}}$，且 $\frac{d}{dx}\Phi(x) = \phi(x)$。
			    利用链式法则：
			    $f_{Z_i}(z) = \frac{d}{dz}\left(2\Phi\left(\frac{z}{\sigma}\right) - 1\right) = 2 \cdot \phi\left(\frac{z}{\sigma}\right) \cdot \frac{d}{dz}\left(\frac{z}{\sigma}\right)$
			    $= 2 \cdot \frac{1}{\sqrt{2\pi}}e^{-\frac{1}{2}\left(\frac{z}{\sigma}\right)^2} \cdot \frac{1}{\sigma}$
			    $= \frac{2}{\sqrt{2\pi}\sigma}e^{-\frac{z^2}{2\sigma^2}}$
			
			所以，$Z_i$ 的概率密度函数为：
			$f_{Z_i}(z) = \begin{cases} \frac{2}{\sqrt{2\pi}\sigma}e^{-\frac{z^2}{2\sigma^2}}, & z \ge 0 \\ 0, & z < 0 \end{cases}$
			这个分布被称为 #折叠正态分布 
- (II) 利用一阶矩求 $\sigma$ 的矩估计量 
	1. [[矩估计法]] 求总体的一阶矩 $E[Z_i]$ 
		1. #期望的定义 $E[g(X)] = \int_{-\infty}^{\infty} g(x)f(x)dx$   #期望公式  对于连续型随机变量：
			$E[Z_i] = \int_{-\infty}^{+\infty} z f_{Z_i}(z) dz$
	2. 当 $z < 0$ 时 $f_{Z_i}(z) = 0$，积分下限可以从0开始：
		$E[Z_i] = \int_{0}^{+\infty} z \cdot \frac{2}{\sqrt{2\pi}\sigma}e^{-\frac{z^2}{2\sigma^2}} dz$
		$= \frac{2}{\sqrt{2\pi}\sigma} \int_{0}^{+\infty} z e^{-\frac{z^2}{2\sigma^2}} dz$
		为了计算这个积分，我们使用 #换元法 。令 $u = \frac{z^2}{2\sigma^2}$，则 $du = \frac{2z}{2\sigma^2}dz = \frac{z}{\sigma^2}dz$，因此 $z dz = \sigma^2 du$。
		当 $z=0$ 时，$u=0$；当 $z \to +\infty$ 时，$u \to +\infty$。
		积分部分变为：
		$\int_{0}^{+\infty} e^{-u} (\sigma^2 du) = \sigma^2 \int_{0}^{+\infty} e^{-u} du = \sigma^2 [-e^{-u}]_{0}^{+\infty} = \sigma^2 (0 - (-1)) = \sigma^2$
		代回原式：
		$E[Z_i] = \frac{2}{\sqrt{2\pi}\sigma} \cdot \sigma^2 = \frac{2\sigma}{\sqrt{2\pi}} = \sigma\sqrt{\frac{2}{\pi}}$
	3. 建立方程并求解 
		1. 矩估计法的核心是令总体矩等于对应的样本矩。
			总体一阶矩：$E[Z_i] = \sigma\sqrt{\frac{2}{\pi}}$
			样本一阶矩：$\bar{Z} = \frac{1}{n}\sum_{i=1}^{n}Z_i$
			令 $E[Z_i] = \bar{Z}$，得到：
			$\sigma\sqrt{\frac{2}{\pi}} = \bar{Z}$
			解出 $\sigma$ 的矩估计量 $\hat{\sigma}$：
			$\hat{\sigma} = \bar{Z}\sqrt{\frac{\pi}{2}} = \frac{\sqrt{2\pi}}{2}\bar{Z}$
			这与答案中的 $\hat{\sigma} = \frac{\sqrt{2\pi}}{2}\bar{Z}$ 一致。
-  (III) 求 $\sigma$ 的最大似然估计量 
	- [[最大似然估计法]]  其核心思想是：对于已知的样本观测值 $(x_1, x_2, \dots, x_n)$，我们选择一个参数 $\theta$ 的值，使得这个样本出现的“可能性”（即似然函数的值）最大
	1. 构建似然函数 $L(\sigma)$ 
		1. 由于样本  $Z_1, Z_2, \dots, Z_n$ 是独立同分布的，所以似然函数是各样本概率密度函数的乘积：
			$L(\sigma) = \prod_{i=1}^{n} f_{Z_i}(z_i; \sigma)$
			假设所有观测值 $z_i \ge 0$，则：
			$L(\sigma) = \prod_{i=1}^{n} \left( \frac{2}{\sqrt{2\pi}\sigma}e^{-\frac{z_i^2}{2\sigma^2}} \right)$
			$= \left(\frac{2}{\sqrt{2\pi}\sigma}\right)^n \prod_{i=1}^{n} e^{-\frac{z_i^2}{2\sigma^2}}$
			$= \left(\frac{2}{\sqrt{2\pi}}\right)^n \sigma^{-n} e^{-\frac{1}{2\sigma^2}\sum_{i=1}^{n}z_i^2}$
	2. 构建对数似然函数 $\ln L(\sigma)$ 
		1. 直接对 $L(\sigma)$ 求导比较复杂，通常我们对其取对数，因为对数函数是单调递增的，最大化 $L(\sigma)$ 等价于最大化 $\ln L(\sigma)$。
			$\ln L(\sigma) = \ln\left[ \left(\frac{2}{\sqrt{2\pi}}\right)^n \sigma^{-n} e^{-\frac{1}{2\sigma^2}\sum_{i=1}^{n}z_i^2} \right]$
			$= n\ln\left(\frac{2}{\sqrt{2\pi}}\right) + \ln(\sigma^{-n}) + \ln\left(e^{-\frac{1}{2\sigma^2}\sum_{i=1}^{n}z_i^2}\right)$
			$= n\ln\left(\frac{2}{\sqrt{2\pi}}\right) - n\ln\sigma - \frac{1}{2\sigma^2}\sum_{i=1}^{n}z_i^2$
	3. 求导并令其为0 
		1. 将对数似然函数对参数 $\sigma$ 求导：
			$\frac{d}{d\sigma}\ln L(\sigma) = \frac{d}{d\sigma}\left[ n\ln\left(\frac{2}{\sqrt{2\pi}}\right) - n\ln\sigma - \frac{1}{2}\left(\sum_{i=1}^{n}z_i^2\right)\sigma^{-2} \right]$
			$= 0 - n\frac{1}{\sigma} - \frac{1}{2}\left(\sum_{i=1}^{n}z_i^2\right)(-2\sigma^{-3})$
			$= -\frac{n}{\sigma} + \frac{1}{\sigma^3}\sum_{i=1}^{n}z_i^2$
			令导数为0，并将 $\sigma$ 替换为其估计量 $\hat{\sigma}$：
			$-\frac{n}{\hat{\sigma}} + \frac{1}{\hat{\sigma}^3}\sum_{i=1}^{n}z_i^2 = 0$
			$\frac{1}{\hat{\sigma}^3}\sum_{i=1}^{n}z_i^2 = \frac{n}{\hat{\sigma}}$
			两边同乘以 $\hat{\sigma}^3$（假设 $\hat{\sigma} \ne 0$）：
			$\sum_{i=1}^{n}z_i^2 = n\hat{\sigma}^2$
			解得 $\hat{\sigma}^2$：
			$\hat{\sigma}^2 = \frac{1}{n}\sum_{i=1}^{n}z_i^2$
			所以 $\sigma$ 的最大似然估计量为：
			$\hat{\sigma} = \sqrt{\frac{1}{n}\sum_{i=1}^{n}z_i^2}$
- 衍生 
	- [[估计量的性质]]   [[估计量的评价标准]]
		-    **无偏性**：考察本题中求出的矩估计量 $\hat{\sigma}_{\text{MME}}$ 和最大似然估计量 $\hat{\sigma}_{\text{MLE}}$ 是否是无偏估计。即计算 $E[\hat{\sigma}]$ 是否等于 $\sigma$。通常最大似然估计量是渐进无偏的，但在小样本下可能是有偏的。
	    *   **有效性**：比较这两个估计量的方差 $Var(\hat{\sigma})$，方差更小的估计量更有效。这会涉及到计算估计量的方差，难度更高。
	    *   **相合性（一致性）**：判断当样本量 $n \to \infty$ 时，估计量是否依概率收敛于真实参数 $\sigma$。
	- #枢轴量与置信区间
		- 如果题目是基于原始样本 $X_i$ 来估计 $\sigma^2$，那么枢轴量 $\frac{(n-1)S^2}{\sigma^2} \sim \chi^2(n-1)$ 是一个非常重要的考点，可以用来构造 $\sigma^2$ 或 $\sigma$ 的置信区间。本题基于 $Z_i$ 构造置信区间会更复杂 
	- #贝叶斯估计 
		-  引入 #先验分布 的概念，结合似然函数得到 #后验分布 ，然后以后验分布的期望或众数作为参数的贝叶斯估计。这是与矩估计、最大似然估计并列的另一种重要估计思想。






















































