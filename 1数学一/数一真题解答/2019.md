![[2019年考研数学（一）真题.pdf#page=1&rect=7,664,595,712|2019年考研数学（一）真题, p.1]]
[[Pasted image 20251004090755.png]]
#同阶无穷小 $\lim_{x \to 0} \frac{x - \tan x}{x^k} = C \quad (C \neq 0)$ 
解题方法一：泰勒展开 x->0  $\tan x = x + \frac{1}{3}x^3 + \frac{2}{15}x^5 + o(x^5)$ 
1. [[泰勒展开式]]    $x-\tan x$ ~  
- 解题方法二：洛必达法则 
	- $\lim_{x \to 0} \frac{(x - \tan x)'}{(x^k)'} = \lim_{x \to 0} \frac{1 - \sec^2 x}{kx^{k-1}}$
	- #三角函数恒等式 $1 + \tan^2 x = \sec^2 x$，所以 $1 - \sec^2 x = -\tan^2 x$ 
	- $\lim_{x \to 0} \frac{-x^2}{kx^{k-1}} = \lim_{x \to 0} -\frac{1}{k} x^{2 - (k-1)} = \lim_{x \to 0} -\frac{1}{k} x^{3-k}$ 
	 1.  如果 $3-k > 0$ (即 $k < 3$)，极限值为 $0$。这不满足同阶无穷小的定义（C必须非零）。
	2.  如果 $3-k < 0$ (即 $k > 3$)，极限值为 $\infty$。这也不满足定义。
	3.  如果 $3-k = 0$ (即 $k = 3$)，极限值为 $-\frac{1}{3}$。这是一个不为零的常数，完全符合同阶无穷小的定义。
C 
 - 衍生 
	 - **替换不同的函数**: 题目中的 $x - \tan x$ 可以换成其他常见的形式，考察你对不同函数泰勒展开式的熟悉程度。例如：
	    *   求与 $x - \sin x$同阶的 $x^k$。 (因为 $\sin x = x - \frac{x^3}{3!} + o(x^3)$，所以 $x - \sin x \sim \frac{1}{6}x^3$, $k=3$)
	    *   求与 $\tan x - \sin x$ 同阶的 $x^k$。 (因为 $\tan x - \sin x \sim \frac{1}{2}x^3$, $k=3$)
	    *   求与 $\arcsin x - x$ 同阶的 $x^k$。 (因为 $\arcsin x = x + \frac{1}{6}x^3 + o(x^3)$，所以 $\arcsin x - x \sim \frac{1}{6}x^3$, $k=3$)
	    *   求与 $e^x - 1 - x$ 同阶的 $x^k$。 (因为 $e^x = 1 + x + \frac{x^2}{2!} + o(x^2)$，所以 $e^x - 1 - x \sim \frac{1}{2}x^2$, $k=2$)
	 
	 
	 



![[Pasted image 20251004090421.png]]
[[Pasted image 20251004090807.png]]

[[可微、连续、可导、偏导数存在的关系]]  
 函数 $f(x)$ 在点 $x=a$ 处可导的**充要条件**是左导数和右导数都存在且相等。即 $f'_{-}(a) = f'_{+}(a)$。
1. #导数的定义   
	1. $f'_{-}(0) = \lim_{x \to 0^-} \frac{f(x) - f(0)}{x - 0} = \lim_{x \to 0^-} \frac{-x^2 - 0}{x} = \lim_{x \to 0^-} (-x) = 0$
	    所以，左导数存在且等于 $0$
	2. $f'_{+}(0) = \lim_{x \to 0^+} \frac{f(x) - f(0)}{x - 0} = \lim_{x \to 0^+} \frac{x\ln x - 0}{x} = \lim_{x \to 0^+} \ln x$
	    当 $x$ 从正方向无限趋近于 $0$ 时，$\ln x$ 趋近于 $-\infty$。所以，这个极限不存在
	3. 左、右导数不相等。因此，函数 $f(x)$ 在 $x=0$ 处**不可导**
2. #判断是否为极值点  
	*   左正右负（$f'(x)$ 在 $x_0$ 左侧 $>0$，右侧 $<0$），则 $x_0$ 为极大值点。
	*   左负右正（$f'(x)$ 在 $x_0$ 左侧 $<0$，右侧 $>0$），则 $x_0$ 为极小值点。
	**重要前提**：使用此方法前，需要先确认函数在 $x=0$ 处是否连续。
	*   左极限：$\lim_{x \to 0^-} f(x) = \lim_{x \to 0^-} (-x^2) = 0$。
	*   右极限：$\lim_{x \to 0^+} f(x) = \lim_{x \to 0^+} (x\ln x)$。这是一个 $0 \cdot \infty$ 型的未定式，可以通过洛必达法则计算 $\lim_{x \to 0^+} \frac{\ln x}{1/x} = \lim_{x \to 0^+} \frac{1/x}{-1/x^2} = \lim_{x \to 0^+} (-x) = 0$。
	*   函数值：$f(0) = 0$。
	因为左极限 = 右极限 = 函数值，所以函数在 $x=0$ 处是**连续的**。现在我们可以使用第一导数判别法
	1.  **分析 $x=0$ 左侧的导数符号**：
	    当 $x < 0$ 时，$f(x) = -x^2$，其导数为 $f'(x) = -2x$。
	    因为 $x < 0$，所以 $-2x > 0$。
	    这意味着在 $x=0$ 的左侧邻域内，函数是**单调递增**的。   
	2. **分析 $x=0$ 右侧的导数符号**：
	    当 $x > 0$ 时，$f(x) = x\ln x$，其导数为 $f'(x) = (x)'\ln x + x(\ln x)' = \ln x + 1$。
	    我们需要判断在 $x=0$ 的一个很小的右邻域内 $f'(x)$ 的符号。当 $x \to 0^+$ 时，$\ln x \to -\infty$，所以 $\ln x + 1$ 必然是负数。
	    更精确地，令 $f'(x) = \ln x + 1 = 0$，解得 $x = e^{-1}$。
	    在区间 $(0, e^{-1})$ 内，$\ln x < -1$，所以 $f'(x) = \ln x + 1 < 0$。
	    这意味着在 $x=0$ 的右侧邻域（例如 $(0, e^{-1})$）内，函数是**单调递减**的。 
	3. 函数 $f(x)$ 在 $x=0$ 的左侧单调递增 ($f'(x)>0$)，在右侧单调递减 ($f'(x)<0$)。根据第一导数判别法，函数在 $x=0$ 处取得**极大值**。
	    因此，$x=0$ 是一个**极值点**。

- 衍生 
	- #可微、连续、偏导数存在的关系   
		- [[可微、连续、可导、偏导数存在的关系]]
		    *   可导必连续，但连续不一定可导（如此题）。
		    *   可微与可导等价（对于一元函数）。
		    *   题目可能会让你判断一个函数在某点是“连续但不可导”、“不连续”还是“可导”。
		



![[Pasted image 20251004090427.png]]
[[Pasted image 20251004090814.png]]

[[级数收敛的必要条件]] 
如果级数 $\sum u_n$ 收敛，那么它的一般项 $u_n$ 必须趋向于0，即 $\lim_{n \to \infty} u_n = 0$。
[[几何级数的求和公式]]
1. (D) $\sum_{n=1}^{\infty} (u_{n+1}^2 - u_n^2)$ 
	1. 典型的**伸缩级数**。我们来考察它的部分和 $S_n$： #伸缩级数 [[伸缩级数]]
		$S_n = \sum_{k=1}^{n} (u_{k+1}^2 - u_k^2)$
		展开后得到：
		$S_n = (u_2^2 - u_1^2) + (u_3^2 - u_2^2) + (u_4^2 - u_3^2) + \dots + (u_{n+1}^2 - u_n^2)$
	2. $\lim_{n \to \infty} S_n = \lim_{n \to \infty} (u_{n+1}^2 - u_1^2)$ 
	3. 已经知道 $\lim_{n \to \infty} u_n = a$，所以 $\lim_{n \to \infty} u_{n+1} = a$
		1. $\lim_{n \to \infty} S_n = (\lim_{n \to \infty} u_{n+1})^2 - u_1^2 = a^2 - u_1^2$ 
	4. 因为 $a$ 和 $u_1$ 都是有限值，所以极限 $a^2 - u_1^2$ 是一个有限值。根据级数收敛的定义，该级数收敛。
		**所以选项 (D) 是正确的
2. (A) $\sum_{n=1}^{\infty} \frac{u_n}{n}$  #举反例 
	1.  $u_n = -\frac{1}{n}$级数变为 $\sum_{n=1}^{\infty} (-1)^n \frac{1}{-1/n} = \sum_{n=1}^{\infty} (-1)^n (-n) = \sum_{n=1}^{\infty} (-1)^{n+1} n$。
    这个级数的通项为 $a_n = (-1)^{n+1} n$。显然 $\lim_{n \to \infty} a_n \neq 0$。根据级数收敛的必要条件，该级数发散
3. (C) $\sum_{n=1}^{\infty} (1 - \frac{u_n}{u_{n+1}})$  
	1.  $u_n = -\frac{1}{n}$ 
	2. 级数变为 $\sum_{n=1}^{\infty} (-1)^n \frac{1}{-1/n} = \sum_{n=1}^{\infty} (-1)^n (-n) = \sum_{n=1}^{\infty} (-1)^{n+1} n$。
	    这个级数的通项为 $a_n = (-1)^{n+1} n$。显然 $\lim_{n \to \infty} a_n \neq 0$。根据级数收敛的必要条件，该级数发散。
4. (B) $\sum_{n=1}^{\infty} (-1)^n \frac{1}{u_n}$ 
	1.  $u_n = -\frac{1}{n}$级数变为 $\sum_{n=1}^{\infty} (-1)^n \frac{1}{-1/n} = \sum_{n=1}^{\infty} (-1)^n (-n) = \sum_{n=1}^{\infty} (-1)^{n+1} n$。
	    这个级数的通项为 $a_n = (-1)^{n+1} n$。显然 $\lim_{n \to \infty} a_n \neq 0$。根据级数收敛的必要条件，该级数发散。

- 衍生 
	- 其他形式的 #伸缩级数
		- $\sum_{n=1}^{\infty} (\frac{1}{u_n} - \frac{1}{u_{n+1}})$: 如果 $u_n \neq 0$ 对所有 $n$ 成立，且极限 $a \neq 0$，则这也是一个收敛的伸缩级数，其和为 $\frac{1}{u_1} - \frac{1}{a}$。
	- #莱布尼茨判别法
		-  选项(B)是一个交错级数。莱布尼茨判别法要求级数 $\sum (-1)^n a_n$ 中的 {$a_n$} 单调递减趋于0。在我们的反例 $u_n = -1/n$ 中，级数为 $\sum (-1)^n (-n)$，不满足判别法条件。如果题目条件变为“{$u_n$} 是**正项**单调增加数列且 $\lim u_n = +\infty$”，那么 $\sum (-1)^n \frac{1}{u_n}$ 就满足莱布尼茨判别法而收敛。







![[Pasted image 20251004090432.png]]
![[Pasted image 20251004090821.png]]
[[格林公式]]
- 这道题考察的是 #第二类曲线积分与路径无关的条件 ，这个知识点与 #格林公式 和 #保守场 （或称 #有势场 ） 密切相关。
[[第二类曲线积分与路径无关的条件]] 
在单连通区域 $D$ 内，以下四个命题是等价的：
1.  对于 $D$ 内的任意分段光滑闭合曲线 $C$，环路积分 $\oint_C Pdx + Qdy = 0$。
2.  对于 $D$ 内的任意两点 $A$ 和 $B$，曲线积分 $\int_L Pdx + Qdy$ 的值与路径 $L$ 无关，只取决于起点 $A$ 和终点 $B$。
3.  存在一个函数 $u(x,y)$（称为 #势函数 ），使得 $du = Pdx + Qdy$，即 $P = \frac{\partial u}{\partial x}$ 且 $Q = \frac{\partial u}{\partial y}$。
4.  函数 $P(x,y)$ 和 $Q(x,y)$ 在 $D$ 内具有一阶连续偏导数，并且满足条件 $\frac{\partial P}{\partial y} = \frac{\partial Q}{\partial x}$。
#分段光滑的简单闭合曲线 
1.   #分段光滑曲线的积分 
	1. 第一步：利用等价条件建立方程
		1. 满足条件 $\frac{\partial P}{\partial y} = \frac{\partial Q}{\partial x}$。
			我们先计算 $\frac{\partial Q}{\partial x}$。
			已知 $Q(x,y) = \frac{x}{y^2}$。将 $y$ 视为常数，对 $x$ 求偏导：
			$\frac{\partial Q}{\partial x} = \frac{\partial}{\partial x}(\frac{x}{y^2}) = \frac{1}{y^2}$
		2. 因此，我们需要的函数 $P(x,y)$ 必须满足：
			$\frac{\partial P}{\partial y} = \frac{1}{y^2}$
2. 第二步：检验四个选项 
	*   **(A)** $P(x,y) = y - \frac{x^2}{y^3} = y - x^2y^{-3}$
	    $\frac{\partial P}{\partial y} = \frac{\partial}{\partial y}(y - x^2y^{-3}) = 1 - x^2(-3y^{-4}) = 1 + \frac{3x^2}{y^4}$
	    这不等于 $\frac{1}{y^2}$，所以排除 A。
	*   **(B)** $P(x,y) = \frac{1}{y} - \frac{x^2}{y^3} = y^{-1} - x^2y^{-3}$
	    $\frac{\partial P}{\partial y} = \frac{\partial}{\partial y}(y^{-1} - x^2y^{-3}) = -y^{-2} - x^2(-3y^{-4}) = -\frac{1}{y^2} + \frac{3x^2}{y^4}$
	    这不等于 $\frac{1}{y^2}$，所以排除 B。
	*   **(C)** $P(x,y) = \frac{1}{x} - \frac{1}{y} = \frac{1}{x} - y^{-1}$
	    $\frac{\partial P}{\partial y} = \frac{\partial}{\partial y}(\frac{1}{x} - y^{-1}) = 0 - (-1y^{-2}) = \frac{1}{y^2}$
	    这个选项满足条件 $\frac{\partial P}{\partial y} = \frac{1}{y^2}$。
	*   **(D)** $P(x,y) = x - \frac{1}{y} = x - y^{-1}$
	    $\frac{\partial P}{\partial y} = \frac{\partial}{\partial y}(x - y^{-1}) = 0 - (-1y^{-2}) = \frac{1}{y^2}$
	    这个选项也满足条件 $\frac{\partial P}{\partial y} = \frac{1}{y^2}$。
3. 第三步：考虑函数的 #连续性 
	1. 等价条件成立的 #前提  函数 $P$ 和 $Q$ 及其一阶偏导数必须在整个区域内连续 
		*   区域是整个上半平面（$y > 0$）。
		*   对于选项 (C) $P(x,y) = \frac{1}{x} - \frac{1}{y}$，这个函数在 $x=0$（即 y 轴）上没有定义，是不连续的。而 y 轴是包含在“上半平面”这个区域内的。题目的条件是“对上半平面内的**任意**有向光滑闭曲线”，这个“任意”意味着曲线可以跨过 y 轴。因此，P(x,y) 必须在整个上半平面都连续，选项 (C) 不满足这个要求。
			





![[Pasted image 20251004090438.png]]

![[Pasted image 20251004090831.png]]

[[规范形和标准型（二次型）]]













![[Pasted image 20251004090442.png]]

![[Pasted image 20251004090839.png]]






















![[Pasted image 20251004090448.png]]
![[Pasted image 20251004090845.png]]





















![[Pasted image 20251004090454.png]]
![[Pasted image 20251004090850.png]]
![[Pasted image 20251004090855.png]]




























![[Pasted image 20251004090458.png]]
![[Pasted image 20251004090904.png]]
























![[Pasted image 20251004090513.png]]

![[Pasted image 20251004090910.png]]












![[Pasted image 20251004090517.png]]
![[Pasted image 20251004090916.png]]
















![[Pasted image 20251004090527.png]]
![[Pasted image 20251004090921.png]]


![[Pasted image 20251004090534.png]]

![[Pasted image 20251004090926.png]]
![[Pasted image 20251004090930.png]]















![[Pasted image 20251004090540.png]]
[[Pasted image 20251004090937.png]]












![[Pasted image 20251004090544.png]]
[[Pasted image 20251004090950.png]]










![[Pasted image 20251004090548.png]]
[[Pasted image 20251004091001.png]]
[[Pasted image 20251004091013.png]]












![[Pasted image 20251004090554.png]]
[[Pasted image 20251004091023.png]]











![[Pasted image 20251004090600.png]]
[[Pasted image 20251004091032.png]]
[[Pasted image 20251004091054.png]]
-   #找规律   
1. 证明单调性 
	1. $a_{n+1} - a_n = \int_{0}^{1} x^{n+1} \sqrt{1-x^2} dx - \int_{0}^{1} x^n \sqrt{1-x^2} dx$ 
	2. 利用定积分的线性性质，合并被积函数：
		$a_{n+1} - a_n = \int_{0}^{1} (x^{n+1} - x^n) \sqrt{1-x^2} dx = \int_{0}^{1} x^n(x-1) \sqrt{1-x^2} dx$
		1. 三者相乘得到$x^n(x-1) \sqrt{1-x^2} \le 0$
		2. 所以，$a_{n+1} - a_n < 0$，即$a_{n+1} < a_n$  #定积分的保号性   
		3. 数列{$a_n$}是单调递减的。 
2. 建立递推关系  
	1. #分部积分法   
	2. $a_n = \int_{0}^{1} x^{n-1} (x \sqrt{1-x^2}) dx$ 
		1. $v = \int x \sqrt{1-x^2} dx = -\frac{1}{2} \int \sqrt{1-x^2} d(1-x^2) = -\frac{1}{2} \frac{(1-x^2)^{3/2}}{3/2} = -\frac{1}{3}(1-x^2)^{3/2}$
	3. $a_n = \left[ u \cdot v \right]_0^1 - \int_0^1 v du$
		$a_n = \left[ x^{n-1} \cdot \left(-\frac{1}{3}(1-x^2)^{3/2}\right) \right]_0^1 - \int_0^1 \left(-\frac{1}{3}(1-x^2)^{3/2}\right) (n-1)x^{n-2} dx$
	4. 我们发现，这两个积分正是$a_{n-2}$和$a_n$的定义！
		$a_n = \frac{n-1}{3} (a_{n-2} - a_n)$
	5. $a_n = \frac{n-1}{n+2} a_{n-2}$ (其中$n=2, 3, \dots$) 
 -   (II) 求极限
1. [[夹逼准则（夹逼定理）]] 
2. 从$a_n < a_{n-1}$，两边同除以$a_{n-1}$ (因为$a_{n-1} > 0$)，得到：
    $\frac{a_n}{a_{n-1}} < 1$  
	1.  从$a_{n-1} < a_{n-2}$，两边取倒数，不等号反向：
	    $\frac{1}{a_{n-1}} > \frac{1}{a_{n-2}}$
	    将递推关系$a_n = \frac{n-1}{n+2} a_{n-2}$代入，得到$a_{n-2} = \frac{n+2}{n-1} a_n$。
	    代入不等式$a_{n-1} < a_{n-2}$中：
	    $a_{n-1} < \frac{n+2}{n-1} a_n$
	    两边同除以$a_{n-1}$并乘以$\frac{n-1}{n+2}$：
	    $\frac{n-1}{n+2} < \frac{a_n}{a_{n-1}}$
	2. 综合以上两个不等式，我们得到了一个夹逼结构：
		$\frac{n-1}{n+2} < \frac{a_n}{a_{n-1}} < 1$ 







![[Pasted image 20251004090612.png]]
[[Pasted image 20251004091107.png]]


[[形心的定义]] #形心坐标公式   
- 所以，求解这道题分为四个核心步骤：
	1.  确定$\bar{x}$的值（通常先考虑对称性）。
	2.  计算体积$V$。
	3.  计算关于$y$的一阶矩$\iiint_\Omega y \,dxdydz$。
	4.  计算关于$z$的一阶矩$\iiint_\Omega z \,dxdydz$。

1. 步骤一：利用对称性求$\bar{x}$    #利用对称性计算三重积分  
	1. 该立体由曲面$x^2 + (y-z)^2 = (1-z)^2$与平面$z=0$围成，且限制$0 \le z \le 1$
	2. **对称性分析**：我们观察立体的定义方程$x^2 + (y-z)^2 \le (1-z)^2$（因为是围成的“体”，所以是小于等于）。如果我们将$x$替换为$-x$，方程变为$(-x)^2 + (y-z)^2 = x^2 + (y-z)^2 \le (1-z)^2$，方程形式完全不变
	3. **结论**：这意味着立体$\Omega$关于$yOz$平面（即$x=0$平面）是对称的。对于对称的均匀物体，其形心必定位于对称面上。因此，形心的$x$坐标为0，即$\bar{x}=0$
2. 步骤二：计算体积$V$   $V = \iiint_\Omega dxdydz$ 
	1. **积分次序**：我们将积分次序定为先对$x,y$积分，再对$z$积分。即： #富比尼定理（交换积分次序）  
	    $V = \int_0^1 \left( \iint_{D_z} dxdy \right) dz$ 
	2. 计算体积$V = \iiint_\Omega dxdydz$。我们采用 #切片法 （或称截面法）来计算这个三重积分 [[三重积分的计算方法（切片法）（投影法）]]  
	3.  **计算截面面积**：$D_z$的面积$A(z)$为： 
	    $A(z) = \iint_{D_z} dxdy = \pi R^2 = \pi (1-z)^2$ 
	4. **对$z$积分**：将截面面积代入体积公式进行积分：
	    $V = \int_0^1 A(z) dz = \int_0^1 \pi (1-z)^2 dz$
	    $= \pi \left[ -\frac{(1-z)^3}{3} \right]_0^1$
	    $= \pi \left( -\frac{(1-1)^3}{3} - \left(-\frac{(1-0)^3}{3}\right) \right)$
	    $= \pi \left( 0 + \frac{1}{3} \right) = \frac{\pi}{3}$
	- **几何补充**：这个立体$\Omega$实际上是一个**斜圆锥**。它的顶点在$(0,1,1)$，底面是$z=0$平面上的圆$x^2+y^2=1$。圆锥的高是1，底面半径是1。根据圆锥体积公式$V = \frac{1}{3} \times \text{底面积} \times \text{高} = \frac{1}{3} \pi (1)^2 \cdot 1 = \frac{\pi}{3}$，验证了我们的积分结果是正确的
3. 步骤三：计算$\bar{y}$    [[一阶矩]] 
	1. #计算一阶矩 我们需要计算$M_{xz} = \iiint_\Omega y \,dxdydz$。同样使用切片法：
	    $M_{xz} = \int_0^1 \left( \iint_{D_z} y \,dxdy \right) dz$ 



![[Pasted image 20251004090617.png]]
[[Pasted image 20251004091116.png]]

[[基的定义 ，向量空间与基]] 

#基  #向量在基下的坐标 
一个向量 $\beta$ 在某个基 $\{\alpha_1, \alpha_2, \alpha_3\}$ 下的坐标是 $(k_1, k_2, k_3)^T$，其定义就是：
$\beta = k_1\alpha_1 + k_2\alpha_2 + k_3\alpha_3$

根据题目，$\beta$ 在基 $\{\alpha_1, \alpha_2, \alpha_3\}$ 下的坐标为 $(b, c, 1)^T$。所以我们有：
$\beta = b\alpha_1 + c\alpha_2 + 1\cdot\alpha_3$

1. 建立向量方程  
	1. 将各个向量的坐标代入上述方程：
	    $(1, 1, 1)^T = b(1, 2, 1)^T + c(1, 3, 2)^T + (1, a, 3)^T$ 
2. **转换为线性方程组**
	将向量方程按分量展开，得到一个关于 $a, b, c$ 的三元线性方程组：
	$$
	\begin{cases}
	1 \cdot b + 1 \cdot c + 1 = 1 \\
	2 \cdot b + 3 \cdot c + a = 1 \\
	1 \cdot b + 2 \cdot c + 3 = 1
	\end{cases}
	$$
3.      $$
    \begin{cases}
    b + c = 0 \quad &(1) \\
    2b + 3c + a = 1 \quad &(2) \\
    b + 2c = -2 \quad &(3)
    \end{cases}
    $$
	1. **求解方程组**
	    *   由方程 (1) 可得 $b = -c$。
	    *   将 $b = -c$ 代入方程 (3)：$(-c) + 2c = -2$，解得 $c = -2$。
	    *   将 $c = -2$ 代回 $b = -c$，解得 $b = 2$。
	    *   将 $b = 2, c = -2$ 代入方程 (2)：$2(2) + 3(-2) + a = 1$，即 $4 - 6 + a = 1$，解得 $a = 3$。
- II  证明 $\{\alpha_2, \alpha_3, \beta\}$ 为 $\mathbb{R}^3$ 的一个基，并求 #过渡矩阵
	- 任务 1：证明 $\{\alpha_2, \alpha_3, \beta\}$ 是一个基 
	- 在 $n$ 维向量空间中（本题是 $\mathbb{R}^3$，所以 $n=3$），任意 $n$ 个线性无关的向量都可以构成该空间的一个基。要证明 $\{\alpha_2, \alpha_3, \beta\}$ 是 $\mathbb{R}^3$ 的一个基，我们只需证明这三个向量是线性无关的
1. 判定线性无关的方法   #证明是否是一个基 
	1.  判断 $n$ 个 $n$ 维向量是否线性无关，最直接的方法是：将这些向量作为列（或行）构建一个方阵，计算其行列式。
	    *   若行列式不等于 0，则向量组线性无关。
	    *   若行列式等于 0，则向量组线性相关。
 2. 构建行列式并计算 
	 1. 求出 $a=3$，所以 $\alpha_3 = (1, 3, 3)^T$。
	    将 $\alpha_2, \alpha_3, \beta$ 作为列向量构建矩阵并计算其行列式：  
	 2.     $$\det(\alpha_2, \alpha_3, \beta) =
    \begin{vmatrix}
    1 & 1 & 1 \\
    3 & 3 & 1 \\
    2 & 3 & 1
    \end{vmatrix}$$
	 3. 按第一行展开计算：
	    $= 1 \cdot \begin{vmatrix} 3 & 1 \\ 3 & 1 \end{vmatrix} - 1 \cdot \begin{vmatrix} 3 & 1 \\ 2 & 1 \end{vmatrix} + 1 \cdot \begin{vmatrix} 3 & 3 \\ 2 & 3 \end{vmatrix}$ 
	 4. $= 1 \cdot (0) - 1 \cdot (1) + 1 \cdot (3) = 2$ 
3. 因为行列式的值为 $2 \neq 0$，所以向量组 $\{\alpha_2, \alpha_3, \beta\}$ 线性无关。因此，它们构成了 $\mathbb{R}^3$ 的一个基。  
- 任务 2：求从基 $\{\alpha_2, \alpha_3, \beta\}$ 到基 $\{\alpha_1, \alpha_2, \alpha_3\}$ 的 #过渡矩阵 
	- [[过渡矩阵的定义]]  
1. 在本题中，$B_{old} = \{\alpha_2, \alpha_3, \beta\}$，$B_{new} = \{\alpha_1, \alpha_2, \alpha_3\}$。
	我们需要求出矩阵 $P$，使得：
	$(\alpha_2, \alpha_3, \beta) = (\alpha_1, \alpha_2, \alpha_3)P$
 2. 表示 $\alpha_2$ 
	 1. $\alpha_2 = 0 \cdot \alpha_1 + 1 \cdot \alpha_2 + 0 \cdot \alpha_3$。
	    所以 $\alpha_2$ 在基 $\{\alpha_1, \alpha_2, \alpha_3\}$ 下的坐标是 $(0, 1, 0)^T$。这是 $P$ 的第一列。
3. 表示 $\alpha_3$
	1.   $\alpha_3 = 0 \cdot \alpha_1 + 0 \cdot \alpha_2 + 1 \cdot \alpha_3$。
	    所以 $\alpha_3$ 在基 $\{\alpha_1, \alpha_2, \alpha_3\}$ 下的坐标是 $(0, 0, 1)^T$。这是 $P$ 的第二列。
4. 表示 $\beta$  
	1. 根据第 (I) 问的结论，我们知道 $\beta = b\alpha_1 + c\alpha_2 + 1\cdot\alpha_3$，其中 $b=2, c=-2$。
	    所以 $\beta = 2\alpha_1 - 2\alpha_2 + 1\alpha_3$。
	    所以 $\beta$ 在基 $\{\alpha_1, \alpha_2, \alpha_3\}$ 下的坐标是 $(2, -2, 1)^T$。这是 $P$ 的第三列。
5. 组合成 #过渡矩阵  #疑惑  #较复杂 
	1. 将上述坐标向量作为列，得到所求的过渡矩阵 $P$：
	    $P = \begin{pmatrix} 0 & 0 & 2 \\ 1 & 0 & -2 \\ 0 & 1 & 1 \end{pmatrix}$
 
	2. **关于图片中解答的说明：**  解答求出的矩阵是 
$$
\begin{pmatrix} 1 & 1 & 0 \\ -\frac{1}{2} & 0 & 1 \\ \frac{1}{2} & 0 & 0 \end{pmatrix}
$$
	这个矩阵是 **从基 $\{\alpha_1, \alpha_2, \alpha_3\}$ 到基 $\{\alpha_2, \alpha_3, \beta\}$ 的过渡矩阵**，也就是所求矩阵的逆矩阵 $P^{-1}$。
	它的推导过程是：
	从 $\beta = 2\alpha_1 - 2\alpha_2 + \alpha_3$ 出发，解出 $\alpha_1$：
	$2\alpha_1 = 2\alpha_2 - \alpha_3 + \beta \implies \alpha_1 = 1\cdot\alpha_2 - \frac{1}{2}\alpha_3 + \frac{1}{2}\beta$
	而 $\alpha_2 = 1\cdot\alpha_2 + 0\cdot\alpha_3 + 0\cdot\beta$
	$\alpha_3 = 0\cdot\alpha_2 + 1\cdot\alpha_3 + 0\cdot\beta$
	将这三个旧基向量在新基下的坐标作为列，就得到了图片中的矩阵
- 衍生 
	- #逆过渡矩阵 
		- 如此题解答所示，经常会考察两个基之间相互转换的关系。从基 A 到基 B 的过渡矩阵 $P_{A \to B}$ 和从基 B 到基 A 的过渡矩阵 $P_{B \to A}$ 互为逆矩阵，即 $P_{A \to B} = (P_{B \to A})^{-1}$
	- #坐标变换公式   #坐标变换  
		- 如果已知一个向量 $X$ 在基 $B_{old}$ 下的坐标为 $K_{old}$，那么它在基 $B_{new}$ 下的坐标 $K_{new}$ 可以通过过渡矩阵 $P = P_{old \to new}$ 来计算：$K_{new} = P^{-1} K_{old}$。或者，如果已知 $K_{new}$，则 $K_{old} = P K_{new}$。
	- #向量组的秩与线性相关性  
		- 通过计算向量组构成的矩阵的秩，来判断其线性相关性，并求其极大线性无关组
	- #向量空间的基与维数 
		- 求一个由向量组生成的子空间的基和维数。
	- #标准正交基 
		- 对一个给定的基，使用施密特（Gram-Schmidt）正交化方法，将其化为标准正交基
	- 不同基下的线性变换矩阵 
		- 如果已知线性变换 $T$ 在基 $B_1$ 下的矩阵是 $A$，求 $T$ 在另一个基 $B_2$ 下的矩阵 $B$。它们之间的关系是 $B = P^{-1}AP$，其中 $P$ 是从基 $B_1$ 到基 $B_2$ 的过渡矩阵。这被称为 #矩阵的相似变换  #相似变换矩阵  #相似变换 





![[Pasted image 20251004090622.png]]
[[Pasted image 20251004091124.png]]
[[Pasted image 20251004091134.png]]

- [[矩阵相似的定义，性质]]    
1.  $|A| = |B|$ 行列式相等
	1. $|A| = -2 \cdot \begin{vmatrix} -2 & -2 \\ 2 & x \end{vmatrix} = -2(-2x - (-2)(2)) = -2(-2x + 4) = 4x - 8$
	    计算矩阵 $B$ 的行列式（$B$ 是一个上三角矩阵，其行列式等于主对角线元素的乘积）：
	    $|B| = 2 \cdot (-1) \cdot y = -2y$
	    因为 $A \sim B$，所以 $|A| = |B|$，得到第二个方程：
	    $4x - 8 = -2y \implies 2x + y = 4$
 2. 迹相等: $\text{tr}(A) = \text{tr}(B)$。矩阵的迹是指主对角线上元素的和。  
	 1. 矩阵 $A$ 的迹为 $tr(A) = -2 + x + (-2) = x - 4$。
		矩阵 $B$ 的迹为 $tr(B) = 2 + (-1) + y = y + 1$。
		因为 $A \sim B$，所以 $tr(A) = tr(B)$，得到第一个方程：
		$x - 4 = y + 1 \implies x - y = 5$
		$\begin{cases} x - y = 5 \\ 2x + y = 4 \end{cases}$
	    将两式相加，得 $3x = 9$，解得 $x=3$。
	    将 $x=3$ 代入第一个方程，得 $3 - y = 5$，解得 $y=-2$
	

-   (II) 求解可逆矩阵 P 
1. **基本思路:**
	如果矩阵 $A$ 和 $B$ 都可被对角化，并且它们相似，那么它们可以被对角化为**同一个**对角矩阵 $\Lambda$（对角线元素为它们的共同特征值）。
	也就是说，存在可逆矩阵 $P_1$ 和 $P_2$ 使得：
	$P_1^{-1}AP_1 = \Lambda$
	$P_2^{-1}BP_2 = \Lambda$
	于是，我们可以得到 $P_1^{-1}AP_1 = P_2^{-1}BP_2$。
	通过移项变换，我们可以构造出我们想要的 $P$
2. 求 A 和 B 的 #共同特征值 
	1. 因为 $A \sim B$，它们有相同的特征值。矩阵 $B$ 是上三角矩阵，其特征值就是主对角线上的元素。
	2. $B = \begin{pmatrix} 2 & 1 & 0 \\ 0 & -1 & 0 \\ 0 & 0 & -2 \end{pmatrix}$
	    所以特征值为 $\lambda_1 = 2, \lambda_2 = -1, \lambda_3 = -2$
3. 求矩阵 A 的特征向量，构造 P₁   [[如何求特征值]]  #求解每个特征值对应的特征向量  
	1.  $x=3$ 代入 $A$
	2.  $A = \begin{pmatrix} -2 & -2 & 1 \\ 2 & 3 & -2 \\ 0 & 0 & -2 \end{pmatrix}$
	3. $\lambda_1 = 2$，解线性方程组 $(A - 2I)\xi = 0$：
        $\begin{pmatrix} -4 & -2 & 1 \\ 2 & 1 & -2 \\ 0 & 0 & -4 \end{pmatrix} \xi = 0 \implies \begin{cases} -4x_1 - 2x_2 + x_3 = 0 \\ 2x_1 + x_2 - 2x_3 = 0 \\ -4x_3 = 0 \end{cases}$
        解得基础解系为 $\xi_1 = (1, -2, 0)^T$。
    -   对应 $\lambda_2 = -1$，解线性方程组 $(A - (-1)I)\xi = 0$：
        $\begin{pmatrix} -1 & -2 & 1 \\ 2 & 4 & -2 \\ 0 & 0 & -1 \end{pmatrix} \xi = 0 \implies \begin{cases} -x_1 - 2x_2 + x_3 = 0 \\ 2x_1 + 4x_2 - 2x_3 = 0 \\ -x_3 = 0 \end{cases}$
        解得基础解系为 $\xi_2 = (-2, 1, 0)^T$。
    -   对应 $\lambda_3 = -2$，解线性方程组 $(A - (-2)I)\xi = 0$：
        $\begin{pmatrix} 0 & -2 & 1 \\ 2 & 5 & -2 \\ 0 & 0 & 0 \end{pmatrix} \xi = 0 \implies \begin{cases} -2x_2 + x_3 = 0 \\ 2x_1 + 5x_2 - 2x_3 = 0 \end{cases}$
        解得基础解系为 $\xi_3 = (1, -2, -4)^T$。
	        将这些特征向量按特征值的顺序 ($\lambda_1, \lambda_2, \lambda_3$) 排列成列向量，构成矩阵 $P_1$：
	    $P_1 = (\xi_1, \xi_2, \xi_3) = \begin{pmatrix} 1 & -2 & 1 \\ -2 & 1 & -2 \\ 0 & 0 & -4 \end{pmatrix}$
	    此时，我们有 $P_1^{-1}AP_1 = \Lambda = \begin{pmatrix} 2 & 0 & 0 \\ 0 & -1 & 0 \\ 0 & 0 & -2 \end{pmatrix}$。
4. 求矩阵 B 的特征向量，构造  P₂
    -   对应 $\lambda_1 = 2$，解 $(B - 2I)\eta = 0$：
        $\begin{pmatrix} 0 & 1 & 0 \\ 0 & -3 & 0 \\ 0 & 0 & -4 \end{pmatrix} \eta = 0 \implies$ 解得 $\eta_1 = (1, 0, 0)^T$。
    -   对应 $\lambda_2 = -1$，解 $(B - (-1)I)\eta = 0$：
        $\begin{pmatrix} 3 & 1 & 0 \\ 0 & 0 & 0 \\ 0 & 0 & -1 \end{pmatrix} \eta = 0 \implies$ 解得 $\eta_2 = (1, -3, 0)^T$。
    -   对应 $\lambda_3 = -2$，解 $(B - (-2)I)\eta = 0$：
        $\begin{pmatrix} 4 & 1 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & 0 \end{pmatrix} \eta = 0 \implies$ 解得 $\eta_3 = (0, 0, 1)^T$。
	- 按特征值的顺序 ($\lambda_1, \lambda_2, \lambda_3$) 排列，构成矩阵 $P_2$：
	    $P_2 = (\eta_1, \eta_2, \eta_3) = \begin{pmatrix} 1 & 1 & 0 \\ 0 & -3 & 0 \\ 0 & 0 & 1 \end{pmatrix}$
	    此时，我们有 $P_2^{-1}BP_2 = \Lambda = \begin{pmatrix} 2 & 0 & 0 \\ 0 & -1 & 0 \\ 0 & 0 & -2 \end{pmatrix}$。
5. 建立 P 与 P₁, P₂ 的关系并求解 
	1. 有 $P_1^{-1}AP_1 = P_2^{-1}BP_2$。
	    我们的目标是得到形如 $P^{-1}AP = B$ 的式子。对上式进行变换：
	    左乘 $P_2$：$P_2P_1^{-1}AP_1 = BP_2$
	    右乘 $P_2^{-1}$：$P_2P_1^{-1}AP_1P_2^{-1} = B$
	    整理一下：$(P_2P_1^{-1})A(P_1P_2^{-1}) = B$
	    令 $P = P_1P_2^{-1}$，那么 $P^{-1} = (P_1P_2^{-1})^{-1} = (P_2^{-1})^{-1}P_1^{-1} = P_2P_1^{-1}$。
	    代入上式，即得 $P^{-1}AP = B$。
	2. 计算 P = P₁P₂⁻¹ 
		1.  求 $P_2$ 的逆矩阵 $P_2^{-1}$。
		    $P_2 = \begin{pmatrix} 1 & 1 & 0 \\ 0 & -3 & 0 \\ 0 & 0 & 1 \end{pmatrix}$
		    使用伴随矩阵法或初等行变换法，可求得：
		    $P_2^{-1} = \begin{pmatrix} 1 & 1/3 & 0 \\ 0 & -1/3 & 0 \\ 0 & 0 & 1 \end{pmatrix}$
		    然后计算矩阵乘法：
		    $P = P_1P_2^{-1} = \begin{pmatrix} 1 & -2 & 1 \\ -2 & 1 & -2 \\ 0 & 0 & -4 \end{pmatrix} \begin{pmatrix} 1 & 1/3 & 0 \\ 0 & -1/3 & 0 \\ 0 & 0 & 1 \end{pmatrix}$
		    $P = \begin{pmatrix} 1 & 1/3+2/3 & 1 \\ -2 & -2/3-1/3 & -2 \\ 0 & 0 & -4 \end{pmatrix} = \begin{pmatrix} 1 & 1 & 1 \\ -2 & -1 & -2 \\ 0 & 0 & -4 \end{pmatrix}$

- 衍生 
	-  矩阵可 #对角化的充要条件    
	- 实对称矩阵的对角化
		-  如果矩阵 $A$ 是一个实对称矩阵 ($A^T = A$)，那么 $A$ 一定可以对角化。
	    *   更进一步，存在一个**正交矩阵** $Q$ ($Q^{-1} = Q^T$)，使得 $Q^TAQ = \Lambda$。这被称为正交对角化。
	    *   考点：如何求这个正交矩阵 $Q$？（需要将特征向量单位化，如果存在重根，还需对同一特征子空间内的特征向量进行施密特正交化） [[正交对角化]]  [[单位化]]  [[施密特正交化]]  
	* 利用对角化计算矩阵的高次幂  #计算矩阵的高次幂  
		* 如果 $P^{-1}AP = \Lambda$，那么 $A = P\Lambda P^{-1}$。
	    *   $A^k = (P\Lambda P^{-1})^k = P\Lambda^k P^{-1}$。
	    *   由于 $\Lambda$ 是对角矩阵，$\Lambda^k$ 非常容易计算（只需将对角元k次方），这大大简化了 $A^k$ 的计算。
	* 判断矩阵是否相似  
		* 判断两个矩阵是否相似的**充要条件**是它们具有相同的 #若尔当标准型 
		*  对于可对角化的矩阵来说，判断它们是否相似，就等价于判断它们是否具有完全相同的特征值（包括重数）。
	    *   对于不可对角化的矩阵，则需要分析其若尔当块的结构。
	- #二次型 
		-   矩阵的对角化，特别是实对称矩阵的正交对角化，是化简二次型 $f(x_1, ..., x_n) = X^TAX$ 为标准型（只含平方项）的核心步骤。
	    *   考点：写出二次型的矩阵表示，通过正交变换化为标准型，并判断二次型的正定性。
![[Pasted image 20251004090629.png]]
[[Pasted image 20251004091158.png]]
- **X 服从参数为 1 的指数分布 ($X \sim E(1)$)**: 我们需要知道指数分布的关键性质：  #指数分布的性质 
    *   **概率密度函数 (PDF)**: $f_X(x) = \begin{cases} \lambda e^{-\lambda x} & x \ge 0 \\ 0 & x < 0 \end{cases}$。在本题中 $\lambda=1$，所以 $f_X(x) = e^{-x}$ for $x \ge 0$。
    *   **累积分布函数 (CDF)**: $F_X(x) = P(X \le x) = \begin{cases} 1 - e^{-\lambda x} & x \ge 0 \\ 0 & x < 0 \end{cases}$。在本题中 $F_X(x) = 1 - e^{-x}$ for $x \ge 0$。
    *   **期望**: $E(X) = \frac{1}{\lambda} = 1$。
    *   **方差**: $D(X) = \frac{1}{\lambda^2} = 1$。
    *   #二阶矩 : $E(X^2) = D(X) + [E(X)]^2 = 1 + 1^2 = 2$。
- **Y 的概率分布**: 这是一个两点分布。 #离散分布的性质 
	*   $P(Y=-1) = p$
	*   $P(Y=1) = 1-p$
	*   **期望**: $E(Y) = (-1) \cdot p + (1) \cdot (1-p) = 1-2p$。
	*   **二阶矩**: $E(Y^2) = (-1)^2 \cdot p + (1)^2 \cdot (1-p) = p + 1 - p = 1$。
1. [[概率密度函数的性质]]    [[概率公式]]   #相互独立事件     #乘积的分布 
	1. 求 Z 的分布函数 $F_Z(z) = P(Z \le z)$
		1. 由于 Y 的取值是离散的，我们可以使用[[全概率公式]] ，$P(AB) = P(AB|C)P(C) + P(AB|\bar{C})P(\bar{C})$ 
			1. 按 Y 的取值进行分类讨论： 
				$F_Z(z) = P(Z \le z) = P(XY \le z)$
				$= P(XY \le z | Y=-1)P(Y=-1) + P(XY \le z | Y=1)P(Y=1)$
			2. 将 $Y$ 的值代入：
				$= P(-X \le z) \cdot p + P(X \le z) \cdot (1-p)$
				整理一下不等式：
				$= p \cdot P(X \ge -z) + (1-p) \cdot P(X \le z)$
		2. 需要根据 $z$ 的取值范围来计算这个概率  
			1. 当 $z < 0$ 时: 
				*   $-z > 0$。
			    *   $P(X \ge -z) = 1 - P(X < -z) = 1 - F_X(-z) = 1 - (1 - e^{-(-z)}) = e^z$。
			    *   因为 $X$ 是非负的 ($X \ge 0$)，而 $z < 0$，所以 $P(X \le z) = 0$。
			    *   代入得：$F_Z(z) = p \cdot e^z + (1-p) \cdot 0 = pe^z$。
			2. 当 $z \ge 0$ 时 
			    *   $-z \le 0$。
			    *   因为 $X$ 总是非负的，所以 $P(X \ge -z) = 1$。
			    *   $P(X \le z) = F_X(z) = 1 - e^{-z}$。
			    *   代入得：$F_Z(z) = p \cdot 1 + (1-p) \cdot (1 - e^{-z}) = p + 1 - p - (1-p)e^{-z} = 1 - (1-p)e^{-z}$。
			综上，Z 的分布函数为：
			$F_Z(z) = \begin{cases} pe^z & z < 0 \\ 1 - (1-p)e^{-z} & z \ge 0 \end{cases}$
			
	2. 求 Z 的概率密度函数 $f_Z(z) = F_Z'(z)$  
		1. 对分段的分布函数求导：
			*   当 $z < 0$ 时，$f_Z(z) = \frac{d}{dz}(pe^z) = pe^z$。
			*   当 $z \ge 0$ 时，$f_Z(z) = \frac{d}{dz}(1 - (1-p)e^{-z}) = -(1-p)(-e^{-z}) = (1-p)e^{-z}$。
		2. 所以，Z 的概率密度函数为：
			$f_Z(z) = \begin{cases} pe^z & z < 0 \\ (1-p)e^{-z} & z \ge 0 \end{cases}$ 
	 **注**: 这个分布是 #拉普拉斯分布 (Laplace distribution) ，也叫 #双指数分布
-  (II) p 为何值时, X 与 Z 不相关  
1. 两个随机变量不相关的数学定义是它们的**协方差为零**，即 $Cov(X, Z) = 0$
	[[协方差]]  协方差的计算公式为：$Cov(X, Z) = E(XZ) - E(X)E(Z)$。  
	1.  **$E(X)$**: 已知 $X \sim E(1)$，所以 $E(X) = 1$ 
	2.  **$E(Z)$**: $E(Z) = E(XY)$。因为 X 和 Y 相互独立，所以 $E(XY) = E(X)E(Y)$。
	    *   $E(Y) = 1-2p$ (前面已计算)。
	    *   所以 $E(Z) = 1 \cdot (1-2p) = 1-2p$
	3. **$E(XZ)$**: 将 $Z=XY$ 代入，得到 $E(XZ) = E(X \cdot XY) = E(X^2Y)$。
	    *   因为 X 和 Y 相互独立，所以 $X^2$ 和 Y 也相互独立。
	    *   因此，$E(X^2Y) = E(X^2)E(Y)$。
	    *   $E(X^2) = 2$ (前面已计算)。
	    *   所以 $E(XZ) = 2 \cdot (1-2p) = 2 - 4p$。
		将这些值代入协方差公式：
		$Cov(X, Z) = E(XZ) - E(X)E(Z) = (2-4p) - 1 \cdot (1-2p) = 2 - 4p - 1 + 2p = 1 - 2p$
	令协方差为 0：
	$Cov(X, Z) = 1 - 2p = 0$
	解得 $p = \frac{1}{2}$
	4. 所以，当 $p = \frac{1}{2}$ 时，X 与 Z 不相关 
-  (III) X 与 Z 是否相互独立?  #判断是否相互独立  
	1. 检验是否对所有的 $(x, y)$ 都满足 $P(X=x, Y=y) = P(X=x)P(Y=y)$
	2. 我们来构造一个反例，就像解答中那样：
考虑事件 $\{X \le 1\}$ 和 $\{Z \le -1\}$。

1.  **计算联合概率 $P(X \le 1, Z \le -1)$**
    $P(X \le 1, Z \le -1) = P(X \le 1, XY \le -1)$
    因为 $X$ 是指数分布，所以 $X > 0$（严格来说是 $P(X>0)=1$）。
    为了使 $XY \le -1$ 成立，Y 必须为负，即 $Y=-1$。
    所以，这个事件可以改写为：
    $P(X \le 1, X(-1) \le -1, Y=-1) = P(X \le 1, -X \le -1, Y=-1)$
    $-X \le -1$ 等价于 $X \ge 1$。
    所以，事件变为 $P(X \le 1, X \ge 1, Y=-1)$。
    $X \le 1$ 和 $X \ge 1$ 同时成立的唯一可能是 $X=1$。
    因为 X 是连续型随机变量，它在任意单点的概率为 0，即 $P(X=1)=0$。
    因此，$P(X \le 1, Z \le -1) = P(X=1, Y=-1) = 0$。

2.  **计算概率的乘积 $P(X \le 1) \cdot P(Z \le -1)$**
    *   $P(X \le 1) = F_X(1) = 1 - e^{-1}$。这是一个大于 0 的数。
    *   $P(Z \le -1) = F_Z(-1)$。根据第一问的结果，当 $z < 0$ 时，$F_Z(z)=pe^z$。
    *   所以 $P(Z \le -1) = F_Z(-1) = pe^{-1}$。
    *   题目条件 $0 < p < 1$，所以 $pe^{-1}$ 也是一个大于 0 的数。

    因此，它们的乘积 $P(X \le 1)P(Z \le -1) = (1 - e^{-1})(pe^{-1}) > 0$。

3.  **比较结果**
    我们发现 $P(X \le 1, Z \le -1) = 0$，而 $P(X \le 1)P(Z \le -1) > 0$。
    两者不相等。
    $P(X \le 1, Z \le -1) \ne P(X \le 1)P(Z \le -1)$。

因此，X 与 Z **不相互独立**。这个结论对所有 $p \in (0,1)$ 都成立，包括使它们不相关的 $p = \frac{1}{2}$。

- 衍生 
	- 不相关与不独立的辨析
		- **关系**: 相互独立 $\implies$ 不相关。反之不成立。
	    *   **特例**: 如果 (X, Y) 服从**二维正态分布**，那么不相关与相互独立是等价的。这道题的 X 和 Z 显然不构成二维正态分布，所以不相关不意味着独立。
	    *   **考试形式**: 可能会直接问“不相关和独立的关系是什么？请举例说明。” 本题就是一个绝佳的例子
	* 随机变量函数的分布
		* #乘积分布: 本题是 $Z=XY$。
	    *   #和的分布 ($Z=X+Y$): 如果 X 和 Y 独立，其密度函数可以通过**卷积公式**求得：$f_Z(z) = \int_{-\infty}^{\infty} f_X(x)f_Y(z-x)dx$。
	    *   #商的分布 ($Z=Y/X$) 和 #最大/最小值分布 ($Z=\max(X,Y)$, $Z=\min(X,Y)$)  也是常见的考点。求解方法都与本题类似，从分布函数 $F_Z(z)$入手。
	* 题中用到的 $Cov(X, XY) = E(Y)D(X)$ 是一个可以记忆的技巧性结论（在 Y 与 X 独立时成立） 
	


![[Pasted image 20251004090635.png]]

[[Pasted image 20251004091209.png]]
[[Pasted image 20251004091215.png]]


#概率密度函数的归一性  $\int_{-\infty}^{+\infty} f(x) dx = 1$ 
1. 题目给出的概率密度函数 $f(x; \sigma^2)$，我们知道当 $x < \mu$ 时，$f(x) = 0$，所以积分的下限从 $-\infty$ 变为 $\mu$。
    因此，我们有：
    $\int_{-\infty}^{+\infty} f(x; \sigma^2) dx = \int_{\mu}^{+\infty} \frac{A}{\sigma} e^{-\frac{(x-\mu)^2}{2\sigma^2}} dx = 1$
2. #换元积分法   
	1. 令 $t = \frac{x-\mu}{\sigma}$。
	    对它求微分，得到 $dt = \frac{1}{\sigma} dx$。
	    同时，更新积分的上下限：
	    *   当 $x = \mu$ 时，$t = \frac{\mu-\mu}{\sigma} = 0$。
	    *   当 $x \to +\infty$ 时，$t \to +\infty$。
3. 转换积分 
	1. 结果代入原积分式，得到：
	    $1 = \int_{0}^{+\infty} A e^{-\frac{t^2}{2}} dt = A \int_{0}^{+\infty} e^{-\frac{t^2}{2}} dt$ 
4. 利用正态分布的性质求解 
	1. 知道标准正态分布 $N(0, 1)$ 的概率密度函数是 $\phi(z) = \frac{1}{\sqrt{2\pi}}e^{-\frac{z^2}{2}}$  
	2. 在 $(-\infty, +\infty)$ 上的积分为1，即 $\int_{-\infty}^{+\infty} \frac{1}{\sqrt{2\pi}}e^{-\frac{z^2}{2}} dz = 1$。
	    两边乘以 $\sqrt{2\pi}$，得到 $\int_{-\infty}^{+\infty} e^{-\frac{z^2}{2}} dz = \sqrt{2\pi}$。
	    由于 $e^{-\frac{z^2}{2}}$ 是一个偶函数（关于y轴对称），所以它在 $(0, +\infty)$ 上的积分等于在 $(-\infty, +\infty)$ 上积分的一半。
	    因此，$\int_{0}^{+\infty} e^{-\frac{t^2}{2}} dt = \frac{1}{2} \int_{-\infty}^{+\infty} e^{-\frac{t^2}{2}} dt = \frac{\sqrt{2\pi}}{2}$
5. **解出 A**：
    将上一步的结果代入第3步的式子中：
    $1 = A \cdot \frac{\sqrt{2\pi}}{2}$
    解得 $A = \frac{2}{\sqrt{2\pi}} = \sqrt{\frac{4}{2\pi}} = \sqrt{\frac{2}{\pi}}$。
    这与图片中的答案完全一致。
-  (II): 求 $\sigma^2$ 的最大似然估计量 
1.  [[最大似然估计法]] 
	1. 第一步：写出似然函数 $L(\sigma^2)$
		1. $L(\sigma^2) = \prod_{i=1}^{n} f(x_i; \sigma^2) = \prod_{i=1}^{n} \left( \frac{A}{\sigma} e^{-\frac{(x_i-\mu)^2}{2\sigma^2}} \right)$
		    将第一问求出的 $A = \sqrt{\frac{2}{\pi}}$ 和 $\sigma = \sqrt{\sigma^2}$ 代入：
		    $L(\sigma^2) = \prod_{i=1}^{n} \left( \frac{\sqrt{2/\pi}}{\sqrt{\sigma^2}} e^{-\frac{(x_i-\mu)^2}{2\sigma^2}} \right)$
		    $L(\sigma^2) = \left( \sqrt{\frac{2}{\pi}} \right)^n \left( \frac{1}{\sqrt{\sigma^2}} \right)^n \prod_{i=1}^{n} e^{-\frac{(x_i-\mu)^2}{2\sigma^2}}$
		    $L(\sigma^2) = \left( \frac{2}{\pi} \right)^{n/2} (\sigma^2)^{-n/2} e^{-\frac{1}{2\sigma^2}\sum_{i=1}^{n}(x_i-\mu)^2}$
	2. 第二步：取对数，得到对数似然函数 $\ln L(\sigma^2)$
		1. 取对数可以把连乘和指数运算转化为加和与乘法，极大简化求导。
		    $\ln L(\sigma^2) = \ln \left[ \left( \frac{2}{\pi} \right)^{n/2} (\sigma^2)^{-n/2} e^{-\frac{1}{2\sigma^2}\sum_{i=1}^{n}(x_i-\mu)^2} \right]$
		    $\ln L(\sigma^2) = \ln\left(\frac{2}{\pi}\right)^{n/2} + \ln(\sigma^2)^{-n/2} + \ln e^{-\frac{1}{2\sigma^2}\sum_{i=1}^{n}(x_i-\mu)^2}$
		    $\ln L(\sigma^2) = \frac{n}{2}\ln\left(\frac{2}{\pi}\right) - \frac{n}{2}\ln(\sigma^2) - \frac{1}{2\sigma^2}\sum_{i=1}^{n}(x_i-\mu)^2$
		    这与图片中的对数似然函数一致。
	3. 第三步：求导并令其为零 
		1. 将对数似然函数对未知参数 $\sigma^2$ 求导。注意，这里是把 $\sigma^2$ 看作一个整体变量。
		    $\frac{d[\ln L(\sigma^2)]}{d\sigma^2} = \frac{d}{d\sigma^2} \left[ \frac{n}{2}\ln\left(\frac{2}{\pi}\right) - \frac{n}{2}\ln(\sigma^2) - \frac{1}{2\sigma^2}\sum_{i=1}^{n}(x_i-\mu)^2 \right]$
		    第一项是常数，导数为0。第二项 $\frac{d}{d\theta}(-\frac{n}{2}\ln\theta) = -\frac{n}{2\theta}$。第三项 $\frac{d}{d\theta}(-C \cdot \theta^{-1}) = C \cdot \theta^{-2}$。
		    $\frac{d[\ln L(\sigma^2)]}{d\sigma^2} = 0 - \frac{n}{2} \cdot \frac{1}{\sigma^2} - \left(-\frac{1}{2(\sigma^2)^2}\right) \sum_{i=1}^{n}(x_i-\mu)^2$
		    $\frac{d[\ln L(\sigma^2)]}{d\sigma^2} = -\frac{n}{2\sigma^2} + \frac{1}{2\sigma^4}\sum_{i=1}^{n}(x_i-\mu)^2$
		    令导数为0：
		    $-\frac{n}{2\sigma^2} + \frac{1}{2\sigma^4}\sum_{i=1}^{n}(x_i-\mu)^2 = 0$
		    两边同乘以 $2\sigma^4$：
		    $-n\sigma^2 + \sum_{i=1}^{n}(x_i-\mu)^2 = 0$
		    整理得到 $\sigma^2$ 的估计值 $\hat{\sigma}^2$：
		    $\hat{\sigma}^2 = \frac{1}{n}\sum_{i=1}^{n}(x_i-\mu)^2$
	4. **得出最大似然估计量**
		将样本观测值 $x_i$ 替换为随机变量 $X_i$，就得到了 $\sigma^2$ 的最大似然估计量 (MLE)：
	    $\hat{\sigma}^2 = \frac{1}{n}\sum_{i=1}^{n}(X_i-\mu)^2$ 
- 衍生 
	- [[矩估计法]] 
		- 这是另一种常用的点估计方法。考题可能会要求你同时用最大似然法和矩估计法求解，并比较二者结果。矩估计法的思想是令样本矩等于总体矩。例如，令样本一阶矩（样本均值）等于总体一阶矩（期望）。
	- $\mu$ 未知的情况 
		- 本题中 $\mu$ 是已知参数。一个非常经典的变式是 $\mu$ 和 $\sigma^2$ 均未知。此时需要求解一个参数方程组，即分别对 $\mu$ 和 $\sigma^2$ 求偏导，并令它们都等于0。通常情况下，$\mu$ 的最大似然估计量是样本均值 $\bar{X}$，而 $\sigma^2$ 的最大似然估计量是 $\frac{1}{n}\sum_{i=1}^{n}(X_i-\bar{X})^2$。 
	-  分布的截断问题 
		- 本题给出的其实是一个在 $x=\mu$ 处截断的“半个”正态分布。这类截断分布 (Truncated Distribution) 的期望和方差计算会与标准正态分布有所不同，这也是一个潜在的考点。 